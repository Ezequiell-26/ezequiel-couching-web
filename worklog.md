# Worklog — EZEQUIEL COACHING

---
Task ID: 22-a
Agent: Z.ai Code (principal)
Task: Auditoría + mejora incremental de la app (protocolo del usuario). INTERRUMPIDA por incidente: el entorno fue reiniciado y el workspace se perdió.

Work Log:
- Se leyó en vivo (esta sesión, verbatim): src/lib/router.ts, home-view.tsx, navbar.tsx + inventario completo de ~140 archivos.
- El contenedor se reinició; verificado: sin src/, sin package.json, sin prisma/, sin db/. git solo tiene commit inicial (3 archivos). Sin copias en todo el filesystem.
- Decisión: restauración fiel desde evidencia (BLOQUE 0), luego mejoras incrementales por bloques.

Stage Summary:
- worklog.md y PROJECT_STATUS.md recreados (este archivo es nuevo; el original se perdió).
- Todo cambio futuro se commitea a git tras cada bloque (lección del incidente).
- Evidencia clave conservada: ViewIds y hashes exactos, pila de la home, navbar, lista de archivos.

---
Task ID: 22-d
Agent: fullstack-admin
Task: Panel del entrenador (CoachView) + gestión de catálogo (AdminView) + 12 rutas API de administración, con sesión de cookie firmada ya existente (/api/admin/session, intacta).

Work Log:
- Contexto leído: worklog.md, prisma/schema.prisma (Lead, Intake, Message, Order, Product, Plan, Post), src/lib/admin-auth.ts, componentes ui/site, lib/utils, lib/analytics, lib/router y rutas públicas existentes para respetar patrones (Next 16: params como Promise).
- IMPORTANTE (modelo de datos): Intake NO tiene columnas sex/daysPerWeek/equipment en schema.prisma; las vistas/API usan solo los campos reales (name, email, age, heightCm, weightKg, goal, experience, injuries, message, status, createdAt). No se tocaron schema ni otros archivos (db push prohibido).
- 12 rutas API creadas bajo src/app/api/admin/ (todas arrancan con `const authed = await isAdminRequest(); if (!authed) 401`; body SIEMPRE validado con zod; ids validados con Number.isInteger; sin campos sensibles; export const dynamic):
  - leads/route.ts → GET lista desc por createdAt.
  - messages/route.ts → GET lista. messages/[id]/route.ts → PATCH {read?, reply?} (refine: al menos un campo).
  - intakes/route.ts → GET lista. intakes/[id]/route.ts → PATCH {status: nuevo|contactado|cliente|descartado}. intakes/export/route.ts → GET CSV (text/csv; charset=utf-8, Content-Disposition attachment filename="intakes.csv", registros desc, BOM + celdas escapadas).
  - orders/route.ts → GET. orders/[id]/route.ts → PATCH {status: pendiente|pagado|entregado|cancelado}.
  - products/route.ts → GET (TODOS incl. inactive) + POST (zod; slug único → 409 "Ese slug ya existe", detectado con Prisma P2002). products/[id]/route.ts → PUT parcial + DELETE soft (active:false → {ok:true,soft:true}).
  - plans/route.ts → GET + POST {slug,title,level(iniciacion|intermedio|avanzado),weeks(1-52),summary,priceCents?}. plans/[id]/route.ts → PUT + DELETE soft (active:false).
  - posts/route.ts → GET + POST {slug,title,excerpt,body,category,published}. posts/[id]/route.ts → PUT + DELETE soft (published:false → {ok:true,soft:true}).
- coach-view.tsx (CoachView, "use client"): puerta de sesión con GET /api/admin/session; si !configured aviso "Acceso deshabilitado: define ADMIN_PASSWORD (mín. 8 caracteres) en .env" con PlaceholderNote; si !authed formulario login (401 → "Contraseña incorrecta.", 429 → mensaje del servidor); si authed: botón "Salir" (DELETE + refetch), botón "Volver a la web" (useRouter de "@/lib/router") y tabs Resumen|Leads|Mensajes|Cuestionarios|Pedidos. Resumen: PUT /api/admin/session → 4 Cards con counts. Leads: tabla escritorio / cards móvil. Mensajes: badge "nuevo" si !read, marcar leído y nota interna (PATCH). Cuestionarios: cards con todos los datos + Select de estado + enlace <a href="/api/admin/intakes/export" download>Exportar CSV</a>. Pedidos: tabla/cards con Select de estado (PATCH). Nota SMTP visible. LoadingState/EmptyState ("Sin mensajes todavía." etc.)/ErrorState con retry; 401 en tabs → refetch de sesión.
- admin-view.tsx (AdminView, "use client"): misma puerta de sesión (duplicada a propósito, sin contexto compartido). Tabs Productos|Planes|Artículos. Tablas escritorio / cards móvil con Badge activo/publicado. Dialog con formularios: slug autogenerado del título (editable, slugify con NFD), precio en euros → priceCents=Math.round(euros*100), Selects de categoría/nivel, Toggle accesible (role="switch") para active/published, body de artículos textarea grande. POST/PUT/DELETE contra las rutas admin; errores inline (409 → "Ese slug ya existe"); tras cada mutación refetch + toast("Guardado") (+ descripción de borrado suave); track("admin_action",{action}) de "@/lib/analytics" en todas las mutaciones.
- Verificación: `bunx tsc --noEmit | grep -E "(coach-view|admin-view|api/admin)"` → 0 errores en archivos propios (los errores restantes de tsc son de vistas pendientes de otros agentes, ignorados). No se ejecutó dev server, lint ni db push.

Stage Summary:
- Rutas API creadas: GET /api/admin/leads; GET /api/admin/messages; PATCH /api/admin/messages/[id]; GET /api/admin/intakes; PATCH /api/admin/intakes/[id]; GET /api/admin/intakes/export (CSV intakes.csv); GET /api/admin/orders; PATCH /api/admin/orders/[id]; GET+POST /api/admin/products; PUT+DELETE /api/admin/products/[id]; GET+POST /api/admin/plans; PUT+DELETE /api/admin/plans/[id]; GET+POST /api/admin/posts; PUT+DELETE /api/admin/posts/[id]. (/api/admin/session no se tocó.)
- Vistas creadas: src/components/views/coach-view.tsx (CoachView) y src/components/views/admin-view.tsx (AdminView), ya conectadas por site-shell (dynamic import) sin tocarlo.
- Seguridad: todas las rutas admin con isAdminRequest() + zod; soft-delete por defecto ({ok:true,soft:true}); datos honestos, tablas vacías sin datos inventados.
- Pendiente de otros entornos: SMTP para entrega de guía/emails (los leads quedan registrados).

---
Task ID: 22-b
Agent: frontend-content
Task: Restaurar las 14 vistas de contenido en src/components/views/ (coaching, servicio, método, resultados, planes, plan, blog, post, FAQ, contacto, guía, legal, 404, cuestionario) con export/props exactos y datos verbatim de los archivos de contenido.

Work Log:
- Leído worklog.md (Task 22-a) y fuente de datos: site.ts, faqs.ts, legal.ts, quiz.ts (verbatim).
- Leídos componentes reutilizables y su API real: PageHeader, Container, SectionHeading, Reveal, Accordion, Card, Button, Input/Textarea/Select/Label, Badge, toaster, states (Loading/Empty/Error/Success/Skeleton), CTAButton, PlaceholderNote, GuideLeadForm, router (ViewId/params), analytics (track), utils (formatPrice/formatDate), seo (faqJsonLd).
- Leídas APIs reales antes de escribir: /api/plans, /api/plans/[slug], /api/posts, /api/posts/[slug] (404 → {error}), /api/contact ({ok,id}|{error}), /api/intake ({ok,id,estimate:{bmr,tdee}}) + prisma/schema.prisma para tipos (Plan.priceCents nullable, Post.body).
- Escritos los 14 archivos en src/components/views/ con "use client", navegación por hash-router (navigate) y CTAButton con track.
- Verificación: `bunx tsc --noEmit` → 0 errores en los 14 archivos (los 11 errores restantes son de site-shell.tsx y calculadoras-view.tsx, trabajo de otros agentes).

Stage Summary:
- Creados (14): coaching-view (3 servicios completos + problemas/valor + fases resumidas + CTA cuestionario, Reveal), servicio-detail-view (breadcrumb, price Badge, includes completo, for, proceso, FAQ con homeFaqs, CTA cuestionario + contacto outline, 404→EmptyState), metodo-view (timeline numerada + 3 principios + CTA), resultados-view (nota honesta si hay [SUSTITUIR], "qué incluirá esta sección", aviso resultados individuales, SIN testimonios inventados), planes-view (fetch + Skeleton/ErrorState/EmptyState "Todavía no hay planes publicados" + formatPrice), plan-view (404 honesto, incluye genérico honesto, CTA checkout, nota entrega digital), blog-view (chips filtro por categoría, formatDate, estados honestos), post-view (body split \n\n → <p>, volver a blog, siguiente paso), faq-view (agrupado por faqCategories + chips + JSON-LD FAQPage con faqJsonLd/useMemo + CTA contacto), contacto-view (POST /api/contact, sending/ok SuccessNote+reset/error server|red, track contact_submit, PlaceholderNote si email [SUSTITUIR), guia-gratis-view (beneficios + GuideLeadForm compact=false + nota RGPD), legal-view (1 componente para aviso/privacidad/cookies/terminos + PlaceholderNote final, sin siguiente paso), not-found-view (404 font-mono + grid de 8 enlaces, sin siguiente paso), cuestionario-view (wizard 4 pasos guiado por quizSteps, validación por paso con intakeSchema.pick().safeParse, errores inline es-ES, barra de progreso, botones ≥44px, POST /api/intake, éxito con ref# + estimate TMB/TDEE "Mifflin-St Jeor", track intake_submit {steps:4}).
- Decisiones: pick-masks por paso declarados Record<StepId, ZodType> (mapeados a los ids de quizSteps); errores de enum traducidos a es-ES vía mapa; precios placeholder se muestran tal cual con PlaceholderNote (anti-inventar); "Siguiente paso" en todas las vistas excepto not-found y legal; sin dependencias nuevas, sin next/image, mobile-first, sin azul/índigo.

---
Task ID: 22-c
Agent: frontend-tools
Task: Restauración de las 8 vistas de herramientas y comercio de la web EZEQUIEL COACHING en src/components/views/ (calculadoras, contador, progreso, dashboard, tienda, producto, checkout, pedido) sobre Next.js 16 + App Router + TypeScript strict + Tailwind 4.

Work Log:
- Contexto leído: worklog.md (incidente 22-a), src/lib/nutrition.ts (fórmulas reales usadas tal cual), calc-profile.ts, food-diary-store.ts, progress-store.ts, router.ts (hash SPA), analytics.ts (track local), utils.ts (formatPrice/formatDate).
- Componentes reutilizados (leídos antes de usar): site/{page-header,container,section-heading,reveal,placeholder-note,states,cta-button}, ui/{card,button,input,accordion,dialog,badge,toaster}. Sin dependencias nuevas, sin next/image, gráficos en SVG manual.
- calculadoras-view.tsx: hub con 7 tabs (IMC, Calorías/TDEE, Macros, 1RM, Grasa corporal, Agua, Peso ideal). Todas con Label+Input numéricos, nombre de la fórmula visible, nota de limitación "estimación, no diagnóstico". TDEE con BMR Mifflin + comparativa Katch-McArdle si hay % grasa opcional y botón "Usar en las demás herramientas" → saveProfile + toast. Macros con goal Select + 3 cards + barra proporcional CSS. 1RM con Epley/Brzycki/media + tabla 95–75%. Grasa con sub-tabs Navy/Deurenberg mostrando ambos resultados si hay datos. Precarga inputs desde loadProfile() al montar; track("calc_use",{name}) al calcular.
- contador-view.tsx: diario 100% localStorage (ec_food_diary_v1). Navegación de fechas (offset ±1 día), anillo SVG kcal vs objetivo (perfil targetKcal ?? 2000, con nota/CTA a calculadoras si no hay perfil), 4 comidas con entradas y papelera, Dialog "Añadir alimento" (FOOD_DB o custom por 100 g, escala grams/per), totales del día en Badges, barras SVG de los últimos 7 días con línea de objetivo, "Borrar mis datos" con confirmación → localStorage.removeItem + toast, track("diary_add_food").
- progress-view.tsx: tracker local (ec_progress_v1). Peso: form fecha+kg, últimas 10 pesadas con delta ▼/▲ vs entrada anterior, gráfico de línea SVG; sesiones: form manual fecha+título+ejercicios+volumen, listado y volumen de los últimos 7 días. Empty state "No hay suficientes datos todavía" si <2 puntos. Borrado con confirmación → removeItem("ec_progress_v1"). Sin datos inventados: todo empieza en 0.
- dashboard-view.tsx: panel local sin cuentas — saludo neutro ("Tu panel"), accesos rápidos (calculadoras/contador/progreso/planes/faq), "Hoy" con kcal del diario vs profile.targetKcal (CTA a calculadoras si null), último peso con fecha, sugerencias honestas (check-in → cuestionario solo como sugerencia vía flag "ec_intake_done", sin depender de él).
- tienda-view.tsx: GET /api/products, chips de categoría derivadas de los datos + "Todo" (preselección desde prop category), cards → navigate("producto",{slug}), skeletons de carga, ErrorState con retry, EmptyState "El catálogo se está preparando", formatPrice, track("shop_filter",{category}).
- producto-view.tsx: GET /api/products/{slug}; 404 → EmptyState "Producto no disponible" + volver a tienda. OK → PageHeader con breadcrumb, Badge de categoría, precio grande, "Qué incluye" honesto (descarga digital, acceso tras confirmación de pago, soporte email), CTA → navigate("checkout",{slug}), nota de entrega digital + PlaceholderNote si falta fileName.
- checkout-view.tsx: resuelve el artículo probando /api/products/{slug} y, si 404, /api/plans/{slug} (kind derivado). Form nombre+email (autocomplete), POST /api/checkout {slug,kind,customerName,customerEmail}; ok → card de confirmación con número font-mono grande, itemName, importe y paymentNote del server (transferencia + [SUSTITUIR: IBAN]) + enlace a #/pedido y botón a home; error → mensaje. track("checkout_submit",{kind}).
- pedido-view.tsx: form número+email → POST /api/orders/lookup; ok → Card con número, artículo, importe, estado con Badge mapeado (pendiente→secondary, pagado/entregado→default, cancelado→destructive vía clase bg-destructive, ya que Badge no tiene variante destructive) y fecha formatDate; 404 → "No encontramos un pedido con esos datos." track("order_lookup").
- Autoverificación: bunx tsc --noEmit filtrado a los 8 archivos → 0 errores (proyecto completo: 0 líneas de error). No se ejecutó dev server ni lint; no se tocó ningún otro archivo.

Stage Summary:
- Las 8 vistas de Task 22-c están creadas y registradas ya en site-shell.tsx (los dynamic imports existentes resuelven contra estos archivos): #/calculadoras, #/contador-de-calorias, #/progreso, #/dashboard, #/tienda, #/tienda/producto/[slug], #/checkout/[slug] y #/pedido operativas.
- Regla de oro respetada: cero datos inventados (totales en 0, gráficos vacíos con mensaje, sin pedidos/usuarios falsos); datos personales solo en localStorage; fórmulas de nutrition.ts usadas sin modificar.
- Pendiente para otros agentes: vistas restantes del shell (coaching, blog, planes, cuestionario, etc.) y sembrar catálogo (products/plans) para que tienda/checkout muestren contenido real.

---
Task ID: 22-e
Agent: lint-fixer
Task: Dejar `bun run lint` en 0 errores / 0 warnings sin cambiar comportamientos visibles ni romper tipos (`bunx tsc --noEmit` en 0), usando el hook src/hooks/use-async-data.ts, más visualización de los nuevos campos del Intake (sex, daysPerWeek, equipment) en CoachView → tab Cuestionarios.

Work Log:
- Patrón A (fetch en effect con setState síncrono, 16 sitios → 0): reemplazado el patrón { status, data, load, useEffect(void load) } por `useAsyncData<T>(fetcher, deps)` en tienda-view, planes-view, plan-view, post-view, producto-view, blog-view y checkout-view (1 fetch por vista; en checkout la lógica "producto → si 404 plan" quedó encapsulada DENTRO del fetcher, el hook solo ve una promesa; 404 → return null para preservar los estados "missing"/"notfound"), en coach-view (sesión + SummaryTab + LeadsTab + MessagesTab + IntakesTab + OrdersTab: un useAsyncData por componente, deps [onExpired], 401 → onExpired() + throw) y en admin-view (sesión + helper useList reescrito sobre useAsyncData, lo que arregla de una vez ProductsTab/PlansTab/PostsTab). `onRetry={() => void load()}` → `onRetry={reload}` en todos. Migraciones de mutaciones: las listas que antes se parcheaban con setX en handlers (Mensajes/Intakes/Pedidos) ahora combinan data del hook con un registro local `updates` (setState solo en handlers, permitido), y los `load()` tras mutación del admin pasaron a `reload()`; props `onExpired`/`onSuccess` tipadas `() => Promise<void>` → `() => void`.
- Bug propio detectado en la herramienta: use-async-data.ts hacía `fetcherRef.current = fetcher` durante el render → react-hooks/refs. Corregido eliminando la ref y capturando el fetcher en la closure del effect (misma semántica: versión del fetcher del render en que cambian deps). El hook queda sin ningún error.
- Patrón B (sincronización de prop, 2 sitios → 0): tienda-view (setActive según prop category) y AddFoodDialog de contador-view (reset de campos al abrir) migrados al patrón de ajuste en render con estado previo (prevCategory/prevOpen + set en render condicionado), sin useEffect.
- Patrón C (carga de localStorage en effect, 10 sitios → 0): inicialización perezosa `useState(() => loadProfile()/loadDiary()/loadProgress())` y eliminación del effect de carga en las 7 calculadoras (ImcCalc, TdeeCalc, MacrosCalc, OneRmCalc, BodyFatCalc, WaterCalc, IdealWeightCalc), ContadorView (diario + objetivo del perfil), DashboardView (sustituidos 6 estados + flag ready por una única instantánea perezosa con los mismos nombres derivados) y ProgressView. Los setState de handlers (añadir/borrar/persistir) se mantienen intactos; no había effects de guardado síncrono que tocar.
- Limpieza asociada (errores no-set-state que bloqueaban el 0/0): imports sin uso eliminados (LoadingState en planes-view y blog-view; función catLabel muerta en calculadoras-view); en blog-view `posts = data ?? []` envuelto en useMemo para calmar react-hooks/exhaustive-deps.
- ADICIONAL schema ampliado: Intake en coach-view.tsx gana sex/daysPerWeek/equipment en el type y nuevas filas en la ficha de cada cuestionario: Sexo (Hombre/Mujer), Días/semana ("1 día"/"N días") y Material (EQUIPMENT_LABELS: gimnasio→Gimnasio, casa_minimo→"Casa (material mínimo)", peso_corporal→Peso corporal, valor desconocido → mostrado tal cual), intercaladas junto a los campos existentes. GET /api/admin/intakes ya devolvía las columnas (findMany completo); no se tocó schema ni rutas.
- Sin cambios en textos visibles, rutas, APIs, diseño ni en los archivos vetados (router, site-shell, layout, page, globals.css, API routes, schema.prisma, home-view, navbar, footer). guide-lead-form/states/dialog/input/navbar/site-shell/home-view (patrón D) intactos.

Stage Summary:
- `bun run lint` (ESLint 9 flat + eslint-config-next 16): 0 errores, 0 warnings (eran 31 errores al empezar: 27 de react-hooks/set-state-in-effect + 1 react-hooks/refs en use-async-data + 3 de imports/vars sin uso).
- `bunx tsc --noEmit`: 0 errores.
- 14 archivos tocados: src/hooks/use-async-data.ts y 13 vistas (tienda, planes, plan, post, producto, blog, checkout, admin, coach, calculadoras, contador, dashboard, progress).

---
Task ID: 22 (integración principal)
Agent: Z.ai Code (principal)
Task: Restauración completa + verificación E2E de la app tras el incidente del workspace.

Work Log:
- Núcleo restaurado por el principal: router verbatim, navbar verbatim, home verbatim, shell, footer, UI kit, lib (seo, nutrition, admin-auth, rate-limit, stores), 25 rutas API, esquema Prisma.
- 3 subagentes en paralelo: 22-b (14 vistas de contenido), 22-c (8 vistas herramientas/tienda), 22-d (paneles coach/admin + 12 rutas API admin).
- Subagente 22-e: 33 errores de lint (react-hooks/set-state-in-effect) → 0 con hook useAsyncData + inicialización perezosa.
- Correcciones del principal tras integración: sync hashchange en SiteShell (bug crítico: el hash no cambiaba de vista), dynamic() con literales inline (Next 16), eslint flat config, columnas Intake sex/daysPerWeek/equipment (aditivo) + CSV export ampliado.
- E2E con agent-browser: home/coaching/planes/tienda/producto/checkout/pedido/cuestionario/calculadoras/contador/faq/404/coach, IMC=22,9 y BMR=1649/TDEE=2556 verificados, contador persiste tras reload, pedido pendiente honesto, admin 401 sin sesión y 503 sin password, CRUD admin OK, menú móvil, footer, consola limpia, 1280/390/320 sin overflow.
- Datos de prueba borrados: BD entregada vacía (sin intake/order/lead/message).

Stage Summary:
- App restaurada y verificada. lint 0, tsc 0, BD vacía sin datos inventados.
- Pendientes documentados en PROJECT_STATUS.md (SMTP, Stripe, cuentas de cliente, placeholders).
- ADMIN_PASSWORD temporal en .env: ezequiel-temp-2026 (CAMBIAR).

---
Task ID: 23
Agent: main (Z.ai Code)
Task: Guardar la web en GitHub (github.com/Ezequiell-26/ezequiel-couching-web)

Work Log:
- Remoto origin configurado y repo verificado (HTTP 200, vacío).
- Auditoría pre-push: detectados secretos trackeados (.env con DATABASE_URL y ADMIN_PASSWORD) y db/custom.db con datos reales.
- .gitignore ampliado (.env, .env.*, db/*.db, db/*.db-journal, tool-results/, agent-ctx/) + git rm --cached (sin borrar local).
- Escaneo de secretos en src/worklog/scripts: sin hallazgos (gsk/ghp/AKIA/private keys).
- Primer push rechazado 403: token fine-grained sin Contents:write (diagnóstico vía API: identidad OK, ls-remote OK, repo vacío).
- Push exitoso con token clásico (ghp_).
- Detectados ~200MB de caché .next/dev/cache/turbopack en el HISTORIAL (blobs hasta 54.9MB, warning GH001 de GitHub).
- Historial reescrito con git filter-branch --index-filter (rm .next) + gc --prune=now --aggressive; 5 commits conservados (nuevos hashes).
- Force push de historia limpia; verificación vía API: .env y db/custom.db → 404 en remoto, árbol remoto correcto.

Stage Summary:
- Repo público https://github.com/Ezequiell-26/ezequiel-couching-web con main limpio: código fuente, public/, prisma/schema.prisma, docs; SIN secretos, SIN BD, SIN caché de build.
- LOCAL intacto: .env y db/custom.db siguen en disco, la app sigue funcionando igual.
- SEGURIDAD: el token ghp_ fue pegado en chat y usado en URLs de comandos → recomendar revocación tras la sesión (junto al fine-grained anterior).

---
Task ID: 24-b
Agent: Z.ai Code (frontend-content biblioteca)
Task: Dataset completo de ejercicios (~100 reales) + vista BibliotecaView (#/ejercicios) + registro de vistas biblioteca/zona en router/shell/navegación/SEO.

Work Log:
- Leído worklog.md (contexto de Tasks 22-x y stub 24) y verificado en caliente que las APIs paralelas /api/zona/* ya importan @/lib/content/exercises (zona-utils, routines, generate, sets, finish): el contrato de tipos/exports se mantuvo EXACTO y el módulo sigue isomórfico (sin "use client").
- Dataset: src/lib/content/exercises.ts reemplazado con 100 ejercicios reales en español, cobertura por grupo: pecho 12, espalda 16, piernas 20, hombros 12, brazos 14, core 14, full-body 12. Equipos repartidos: mancuernas 30, peso-corporal 32, barra 19, máquinas 17, poleas 15, kettlebell 6, banda 5. Niveles: 44 principiante / 46 intermedio / 10 avanzado. Cada ejercicio con id kebab-case único, primaryMuscles (1-2), secondaryMuscles (1-3), 4-6 pasos de ejecución y 1-2 tips técnicos. Validación scriptada: 0 ids duplicados, 0 violaciones de contrato (acoté a ≤2 músculos primarios/≤3 secundarios en peso muerto, burpees, thruster, clean&press, snatch, get-up).
- Vista nueva src/components/views/biblioteca-view.tsx (BibliotecaView, "use client"): PageHeader + Container, buscador con Input type=search y 3 <select> nativos estilizados (ui/input Select) para grupo/equipo/nivel usando las constantes del módulo; resultados vía filterExercises en useMemo; contador aria-live; grid 1→sm:2→lg:3→xl:4 de Cards clicables (button accesible, aria-haspopup="dialog") con badges de grupo/nivel/equipo y músculos primarios; Dialog con músculos, instrucciones <ol>, tips destacados en volt y CTA "Ir a Mi Zona" (navigate("zona") + track). EmptyState "Prueba con otros filtros" + botón limpiar cuando hasFilters. Analítica con el patrón de la casa: track("biblioteca_filter") en cada cambio de filtro (como shop_filter de tienda) y track("cta_click",{label:"ir-a-mi-zona"}) en el CTA; el page_view ya lo cubre el evento view_change del router.
- Router (edits mínimos): ViewId += "biblioteca" | "zona"; VALID_VIEWS += ambos; parseHash += case "ejercicios" → biblioteca y case "mi-zona" → zona; toHash += case "biblioteca" → "#/ejercicios" y case "zona" → "#/mi-zona" (idempotentes, antes del default).
- Site-shell (edits aditivos): const BibliotecaView = dynamic(...) junto a ContadorView y case "biblioteca" en CurrentView. "zona" deliberadamente sin vista (cae en NotFoundView hasta que el agente paralelo la monte).
- Navbar (edits aditivos): NavItem gana zone?/desktopClassName?; NAV_ITEMS += "Ejercicios" (li hidden lg:block en escritorio) y "Mi Zona" (zone:true → excluida de la lista desktop, mostrada como CTAButton volt view="zona" hidden lg:inline-flex junto a "Empezar"); el menú móvil los hereda automáticamente (map completo de NAV_ITEMS). Por debajo de lg no cambia nada respecto a lo previo (sin saturar a 768-1023px); footer los cubre ahí.
- Footer: columna Herramientas += "Ejercicios" y "Mi Zona" (mismo patrón que los vecinos).
- SEO: seo.ts VIEW_META += biblioteca ("Biblioteca de ejercicios — Ezequiel Coaching") y zona ("Mi Zona de entrenamiento — Ezequiel Coaching") tras "contador" (Record<ViewId,Meta> exige ambas claves); sitemap.ts += "/ejercicios" y "/mi-zona" siguiendo su patrón de hashes.
- Verificación: bunx tsc --noEmit → 0 errores (proyecto completo); bun run lint → 0 errores/0 warnings; GET / → 200 y dev.log limpio; las peticiones del agente paralelo a /api/zona/* siguen en 200 con el dataset cargado.

Stage Summary:
- Creados: src/components/views/biblioteca-view.tsx. Modificados: src/lib/content/exercises.ts (dataset 100 ejercicios, contrato intacto), src/lib/router.ts, src/components/site/site-shell.tsx, src/components/site/navbar.tsx, src/components/site/footer.tsx, src/lib/seo.ts, src/app/sitemap.ts.
- #/ejercicios operativo con búsqueda + 3 filtros y fichas de ejercicio con CTA a Mi Zona; #/mi-zona ya parsea/navega (enlaces en navbar desktop ≥1024px, menú móvil y footer) y muestra 404 honesto hasta que el agente de zona monte su vista.
- Dataset listo para rutinas: 100 ids estables, balance 12/16/20/12/14/14/12 por grupo y 7 tipos de equipo, apto para /api/zona/routines/generate.
- lint 0 · tsc 0 · sin tocar prisma, APIs ni otras vistas.

---
Task ID: 24-c
Agent: backend-zona
Task: Backend completo de la Zona de entrenamiento: lib de sesión de cliente (scrypt + token en BD) y 19 endpoints nuevos bajo /api/zona/* (perfil, rutinas, IA generadora, sesiones/series con PRs, peso/agua/hábitos, progreso agregado y logros). Sin tocar vistas, schema ni rutas existentes.

Work Log:
- Leídos worklog.md, prisma/schema.prisma (modelos zona ya extendidos por 24-db), src/lib/admin-auth.ts, rate-limit.ts, db.ts, api/intake, api/admin/session y api/products/[slug] (patrones: NextResponse.json, zod, dynamic force-dynamic, params Promise, cookies vía res.cookies.set, rateLimit+clientKey por IP).
- z-ai-web-dev-sdk@0.0.18 instalado en el proyecto (dependencia backend-only; era solo global).
- src/lib/zona-auth.ts (nuevo): hashPin (scrypt salt 16B + clave 64B, "salt:hash"), verifyPin (timingSafeEqual, formato inválido → false), ZONA_COOKIE "ec_zona_session" TTL 30 días, createClientSession (token randomBytes(32).hex persistido en BD, invalida el anterior), destroyClientSession, getZonaProfile/requireZonaProfile (cookie → sessionToken → sessionExpiry vigente), zonaUnauthorized() (401 "Iniciá sesión para continuar."), zonaCookieOptions (httpOnly, sameSite strict, secure solo en prod, path /, maxAge 30d), publicProfile() (nunca expone pinHash/sessionToken/sessionExpiry).
- src/lib/zona-utils.ts (nuevo, helpers backend): serializeRoutine/serializeSession/serializeSet (resuelven exerciseName vía getExerciseById), computeVolumeKg (solo series con peso), epley1rm (1 decimal), dateKey/utcMidnight/addDays/startOfUtcWeek/isoWeekKey (todo UTC), computeStreak (hoy o ayer, días consecutivos con actividad).
- 19 endpoints creados (todos con try/catch → 500 "Error interno.", log real solo en console.error):
  - POST /api/zona/profile (registro; rate-limit 5/min por IP; nombre único case-insensitive vía $queryRaw lower(); 409 con mensaje exacto; 201 {id,name}+cookie).
  - GET /api/zona/profile ({id,name}|401), POST .../profile/login (rate-limit 10/5min; 401 genérico "Nombre o PIN incorrecto." sin revelar qué falló; rota token), POST .../profile/logout (revoca en BD + cookie maxAge 0).
  - GET /api/zona/routines (activas, items ordenados day/orderIdx + exerciseName), POST /api/zona/routines (manual; valida cada exerciseId contra la biblioteca → 400 listando ids inválidos; day ≤ daysPerWeek; orderIdx por día; 201 completa).
  - GET/DELETE /api/zona/routines/[id] (owner check por findFirst con profileId de la sesión → 404 sin revelar existencia; DELETE soft active:false → {ok:true}, conserva histórico de sesiones).
  - POST /api/zona/routines/generate (IA real con z-ai-web-dev-sdk; rate-limit 5/min; filtra EXERCISES por equipment —gimnasio→todos, mancuernas/casa/peso-corporal según mapeo— y nivel —principiante→principiante+intermedio—, máx 60 candidatos enviando solo {id,name,group}; SYSTEM en español con shape JSON exacto, thinking disabled, timeout 60s; parse tolerante a fences ```json + zod; exerciseIds inválidos descartados, día <3 ejercicios o days vacío → 502 "La IA generó una rutina inválida. Probá de nuevo."; cualquier error SDK/parse → 503 "El generador IA no está disponible..." sin filtrar detalle; crea Routine source:"ia" con notes de fecha; 201).
  - POST /api/zona/sessions (reanuda la ÚNICA sesión activa → 200 {resumed:true}; si routineId: owner check + title = "rutina · Día N"; default "Entrenamiento"; 201), GET /api/zona/sessions?status=activa|todas&limit≤20 (con setsCount y routineName), GET /api/zona/sessions/[id] (sets asc + nombres).
  - POST /api/zona/sessions/[id]/sets (ejercicio debe existir en biblioteca → 400; sesión completada → 409; PR en vivo contra máximo de sesiones COMPLETADAS → {isPR:true, previousMax}; crea set y recomputa volumeKg en la misma $transaction; 201 {set, volumeKg}), DELETE .../sets/[setId] (owner doble join, recomputa volumen → {ok, volumeKg}).
  - POST /api/zona/sessions/[id]/finish (status completada + finishedAt + volumen final; newPRs = mejor peso de ESTA sesión por ejercicio vs máximo de completadas anteriores — la sesión aún está "activa" al comparar, exclusión natural —, e1rm Epley 1 decimal, sin peso → sin PR; ordena desc).
  - POST /api/zona/weight (kg 30-300, date opcional YYYY-MM-DD → medianoche UTC; upsert compound profileId_date), POST /api/zona/water (ml 50-2000, upsert incremental → total del día), POST /api/zona/habits (enum de 6 hábitos, upsert toggle).
  - GET /api/zona/progress?weeks=8 (clamp 1-12): weights 90d asc, waterToday+waterWeek 7d (grid con ceros), habitsToday+habitsWeek 7d, volumeByWeek semanas ISO desde sets de completadas (bucket por finishedAt), prs top 50 desc por peso (e1rm+fecha), totals {sessionsTotal, sessionsCompleted, totalVolumeKg, prsCount, daysTrainedLast30}, streak (ANY actividad: serie, peso, agua ≥500ml, hábito done) {days,lastActiveDate}, activeSession para resume.
  - GET /api/zona/achievements (10 definiciones estáticas; desbloqueo 100% real: primera-sesion, racha-7, sesiones-10/25 con progreso x/10 y x/25, primer-pr, prs-5, volumen-10t progreso kg/10000, agua-7 progreso racha ≥1,5L/7, peso-4 progreso n/4, rutina-ia; sin datos → unlocked:false y progreso 0).
- Aislamiento por construcción en TODAS las queries: where.profileId derivado de la sesión (nunca del body); owner checks → 404 (no 403) para no revelar existencia.
- Smoke test E2E con curl (18 casos): registro/login/duplicado case-insensitive/PIN erróneo (401 genérico), cookie httpOnly+SameSite=strict+Max-Age 30d verificada, rotación de token invalida sesión previa, peso upsert mismo día, agua incremental 500+750=1250, hábitos toggle, rutina manual 201 + 400 ids inválidos + 400 day overflow, sesión desde rutina con title "· Día 2", PR en vivo (65kg vs previo 60 → isPR + previousMax:60; primer PR → previousMax:null), borrado de serie con recomputo de volumen, finish con newPRs (e1rm 116.7 = 100×(1+5/30) ✓) y 409 al repetir, progreso agregado correcto (semana ISO 2026-W37=1100kg, prs desc, streak 1 día), logros con progreso real (2/10, 1100/10000, 1/4), generador IA real → 201 con rutina de 3 días × 5-6 ejercicios, todos los exerciseIds válidos y material acorde (mancuernas), aislamiento entre perfiles (404), soft-delete, logout + revocación, rate-limit 429 en el 5º registro/minuto.
- INCIDENTE OPERATIVO resuelto: el dev server (arrancado a las 22:45) tenía en memoria el PrismaClient generado ANTES de que 24-db extendiera el schema (db.clientProfile undefined en runtime). No fue un bug de código: el cliente regenerado en disco expone todos los delegados (verificado con script). Se reinició el único proceso del dev server (mismo comando bun run dev, puerto 3000, log en dev.log) para que cargara el cliente regenerado; sin duplicar servidores.
- BD devuelta a estado vacío tras las pruebas: deleteMany de ClientProfile con cascada verificada → las 8 tablas en 0 (ClientProfile, Routine, RoutineItem, WorkoutSession, SetLog, WeightLog, WaterLog, HabitLog). Cero datos demo.

Stage Summary:
- Backend de la Zona operativo: 18 archivos nuevos (src/lib/zona-auth.ts, src/lib/zona-utils.ts y 16 route.ts bajo src/app/api/zona/), 19 endpoints. Rutas existentes, vistas, schema y prisma client intactos (solo se añadió z-ai-web-dev-sdk a dependencies).
- Seguridad: PIN con scrypt+salt y timingSafeEqual; token de sesión aleatorio persistido y rotable (invalida sesiones anteriores); cookies httpOnly/strict; rate-limit por IP en register/login/generate; jamás se devuelven pinHash/sessionToken/sessionExpiry; errores del SDK de IA nunca filtrados al cliente (503 genérico).
- Verificación: bun run lint → 0 errores/0 warnings; bunx tsc --noEmit → 0 errores (proyecto completo). BD entregada vacía; los endpoints responden con estructuras vacías/cero correctas en estado sin datos.
- Nota para el frontend (Task 24-a): las respuestas de zona usan {error} (no {ok:false,error}); 401 texto "Iniciá sesión para continuar."; generate puede responder 502 (contenido inválido) o 503 (servicio indisponible); progress y achievements ya incluyen todo lo necesario para pintar sin llamadas extra.
---
Task ID: 24-d
Agent: frontend-zona
Task: Vista completa de Mi Zona (#/mi-zona) consumiendo las APIs /api/zona/*: AuthGate (crear cuenta/iniciar sesión), 4 tabs (Rutinas · Entrenar · Progreso · Logros) y gráficos SVG propios. Sin datos demo: todo desde las APIs reales, arranca vacío.

Work Log:
- Contexto leído: worklog.md (24-b: registro de vistas/hash #/mi-zona ya existente; 24-c: contratos exactos de los 19 endpoints), zona-utils.ts, zona-auth.ts, las 16 rutas /api/zona/*, site-shell.tsx, states.tsx, contador-view.tsx (patrón toast/toaster, track) y biblioteca-view (patrón selects nativos).
- Contrato verificado EN CALIENTE con smoke E2E (cookie jar): registro 201, rutina manual 201 (400 por exerciseId inválido antes de corregir el id), sesión desde rutina con title "· Día 1", set 201 {set,volumeKg,isPR:true,previousMax:null}, finish 200 {session,newPRs[e1rm:53.3]}, weight {weight:{date,kg}}, water {ml} acumulativo, habits {habit,done}, progress (waterWeek con ceros, habitsWeek, volumeByWeek, streak) y achievements (primera-sesion unlocked). Después de las pruebas: limpieza con PrismaClient deleteMany en cascada → 8 tablas en 0 (sin datos demo).
- Nuevos archivos:
  - src/components/zona/api.ts: DTOs espejo de los serializers (Routine/Session/Set/Progress/Achievement/NewPR), zonaApi<T>() (headers JSON, {error}→Error tal cual, 401→ZonaUnauthorized para volver al AuthGate), labels de goal/level/equipment/hábitos y formateadores es-AR (fmtInt "12.450", fmtKg "62,5", shortDate "12 mar", shortWeek "S37").
  - src/components/zona/charts.tsx: LineChart (polilínea volt, puntos, min/max, último valor destacado, edge de 1 punto y valores iguales) y BarChart (barras volt, etiquetas alternadas si >9), ambos role="img"+aria-label, viewBox responsivo, sin dependencias.
  - src/components/zona/auth-gate.tsx: tabs propias (role=tablist, no hay componente Tabs), nombre + PIN (type=password, inputMode=numeric, maxLength 6, sanitize \D), validación cliente 4-6 dígitos, errores del API verbatim en ErrorState, 1 línea explicando qué guarda la cuenta.
  - src/components/zona/routines-tab.tsx: cards desplegables por estado (aria-expanded/aria-controls) con badges goal/nivel/días/equipo + badge "IA"; por día: ejercicios con series×reps·descanso y botón "Entrenar día N" (POST /sessions con routineId+day → cambia a tab Entrenar); eliminar con Dialog de confirmación; generador IA con 4 <select> nativos estilizados, loading honesto ("puede tardar hasta un minuto"), 502/503 del API mostrados con botón reintentar; éxito → toast + refresh + abre la nueva; EmptyState "Todavía no tenés rutinas" con CTA que scrollea al generador (#zona-generador).
  - src/components/zona/train-tab.tsx: TrainTab (header con título, SessionTimer aislado que re-renderiza solo 1 componente/seg, volumen, nº series; bloques por ejercicio con series planificadas si viene de rutina (busca routineId+day en la lista), chips de series (peso×reps, RPE, badge volt "¡Nuevo PR!" si isPR, borrar → DELETE con recomputo de volumen desde {volumeKg}); formulario inline peso (step 0.5, opcional) + reps + RPE select 1-10 + "Registrar serie"; isPR → toast de celebración + badge persistente en el chip; select con optgroups de EXERCISES para entrenamiento libre; "Finalizar" en header + al fondo + Dialog resumen (volumen/series/duración) → POST finish) y TrainEmpty (EmptyState con CTA "Ir a mis rutinas" + entrenamiento libre: input título → POST /sessions sin routineId, maneja resumed:true).
  - src/components/zona/progress-tab.tsx: 5 stat tiles (sesiones completadas, volumen total es-AR, PRs, racha, días 30d); peso con input + LineChart (aria-label con rango) o EmptyState si vacío; agua con progressbar role + +250/+500 (patch optimista desde {ml} del response + refresh silencioso) y objetivo fijo 2000 ml "objetivo sugerido" (sin datos clínicos); 6 hábitos toggles aria-pressed (POST habits, patch desde response) + mini-grid semanal n/6; volumen semanal con BarChart (EmptyState honesto si todo 0); tabla de PRs ejercicio/peso/e1RM o "Tus récords aparecerán al entrenar con peso".
  - src/components/zona/achievements-tab.tsx: grid sm:2/lg:3, desbloqueados borde+fondo volt con Check, bloqueados atenuados con progressbar current/target cuando existe; contador "x de N" y texto "Empezá a entrenar para desbloquear logros" si 0 desbloqueados.
  - src/components/views/zona-view.tsx (export nombrado ZonaView): fases checking/anon/ready/error; al montar GET /profile (401=AuthGate, no error) con setState solo en continuaciones de promesa (cumple react-hooks/set-state-in-effect); carga paralela progress+routines+achievements; barra de perfil con "Cerrar sesión" (POST logout → reset a AuthGate con mensaje honesto); 4 tabs con role=tablist, dot volt en "Entrenar" si activeSession, track("zona_tab"); activeSession vive en progress (patch optimista al iniciar, refresh en background); startFromRoutine (POST /sessions routineId+day, maneja resumed) → tab Entrenar; al finalizar → pantalla de éxito (SuccessNote + volumen/series/duración + tarjetas PR con e1RM + CTAs a progreso/rutinas) mientras refresh de progress+achievements; handleActionError: ZonaUnauthorized→AuthGate ("Tu sesión expiró…"), resto→toast error; analítica con el patrón de la casa (track de view_change ya cubre page_view): zona_start_session, zona_generate_routine, zona_log_set{pr}, zona_finish_session, zona_log_weight/water, zona_toggle_habit, zona_tab, cta_click{label:"zona-primera-rutina"|"zona-ir-a-rutinas"|"zona-auth-ok"}.
- site-shell.tsx (edits aditivos, patrón exacto): const ZonaView = dynamic(...ssr:false) junto a BibliotecaView + case "zona" en CurrentView (el ViewId y el hash ya existían de 24-b).
- UX/accesibilidad: botones e inputs min-h-11 (44px), grids responsivos 1→2→4, charts w-full sin overflow a 320px, tabla de PRs con overflow-x-auto, errores nunca silenciosos (ErrorState/Toast), sin pantallas blancas (LoadingState en fases asíncronas).
- Verificación: bun run lint → 0 errores/0 warnings (resolví 5 del react-hooks v6: set-state-in-effect migrando a continuaciones .then y quitando props/imports no usados); bunx tsc --noEmit → 0; GET / → 200 y dev.log limpio (solo los smoke tests, todos 2xx); BD devuelta a 0 registros. NO inicié dev server ni toqué router/navbar/footer/seo/APIs/prisma.

Stage Summary:
- Creados: src/components/zona/api.ts, charts.tsx, auth-gate.tsx, routines-tab.tsx, train-tab.tsx, progress-tab.tsx, achievements-tab.tsx y src/components/views/zona-view.tsx. Modificado (aditivo): src/components/site/site-shell.tsx.
- #/mi-zona operativo: AuthGate → tabs Rutinas (lista + IA generador), Entrenar (sesión activa con series/PRs/finalización o entrenamiento libre), Progreso (stats/peso/agua/hábitos/volumen/PRs con charts SVG propios) y Logros; contratos 1:1 con las APIs de 24-c verificados en caliente.
- lint 0 · tsc 0 · sin datos demo (BD en 0) · sin push a git.

---
Task ID: 24-e
Agent: main (Z.ai Code)
Task: Verificación E2E del sistema de entrenamiento (Tasks 24-b/c/d) + fixes + push

Work Log:
- lint 0 / tsc 0 / dev.log limpio.
- E2E agent-browser: biblioteca (búsqueda "sentadilla" → 12 resultados, dialog con músculos/ejecución OK), mi-zona (registro → tabs, login → tabs).
- BUG 1 fix: handleAuthenticated no disparaba loadData → vista colgada en "Cargando…" tras login/registro (zona-view.tsx, +void bootstrap()).
- BUG 2 fix: generate/route 503 por JSON malformado del LLM → repairJson (comas colgantes/comillas tipográficas) + tryParseJson + reintento (2 intentos, 2.º con feedback al modelo).
- Flujos verificados: rutina IA real 3 días; sesión día 1; series 60kg×8 y 40kg×10; finalización con 2 PRs y e1RM Epley exacto (76 / 53,3); progreso: peso 78,5 (curva), agua 500ml (25%), PRs table; logros 3/10 reales.
- 320px sin overflow en las 2 vistas nuevas; 0 errores de página; sin hydration errors.
- BD de prueba eliminada (cascada): 0 perfiles/rutinas/sesiones/sets/logs.

Stage Summary:
- Features de los 20 proyectos integradas y verificadas: biblioteca (100 ejercicios), rutinas + IA, tracking con PRs, peso/agua/hábitos, logros, navegación nueva.
- BD entregada vacía (sin datos demo). Historial de hidration errors en dev.log era transitorio de hot-reload; verificado 0 en E2E final.

---
Task ID: 25-c
Agent: pwa-offline
Task: Service worker offline + registro seguro para PWA

Work Log:
- Contexto leído: worklog.md (Tasks 22 y 24). Estado existente verificado sin recrear: src/app/manifest.ts ya define el manifest con /brand/icon-192.png y /brand/icon-512.png (standalone, #0b0b0e) y layout.tsx ya tenía metadata + viewport (intactos).
- Creado public/sw.js (vanilla JS, VERSION "ec-offline-v1"):
  - install: caches.open(VERSION) → addAll(["/"]) con .catch(() => {}) dentro de event.waitUntil (el install no falla si "/" no responde). Precahe del shell porque la app es SPA por hash: con "/" cacheada, toda navegación #/... funciona offline.
  - activate: borra caches cuyo nombre ≠ VERSION + clients.claim().
  - DECISIÓN de actualización: NO se usa skipWaiting en install (actualización natural: el SW nuevo espera a que no queden pestañas con la versión anterior, evitando mezclar versiones del shell en sesiones en curso). Se elige clients.claim() en activate para controlar pestañas ya abiertas recién activado el SW (primer install); riesgo bajo porque navegaciones son network-first y APIs nunca se interceptan.
  - fetch (respondWith SOLO para GET same-origin): (1) method ≠ GET → passthrough; (2) pathname empieza con "/api/" → NUNCA se intercepta (sin cachear APIs dinámicas/autenticadas); (3) navigate → network-first: fetch → si ok se cachea (put del request clonado) y se devuelve; catch → cache.match("/") → si no hay, Response HTML mínimo "Sin conexión" status 200 text/html; (4) /_next/static/, /brand/, /images/ → cache-first (match → hit devuelve; miss → fetch → put clonado → devolver); (5) resto de GET same-origin → passthrough sin cachear.
  - Solo se cachean respuestas type "basic" (same-origin) con status 200: nunca opacas ni errores. cache.match usa { ignoreVary: true } para tolerar headers Vary de Next. Assets no cacheables ni en caché ni en red → 504 mínimo sintético.
  - Sintaxis verificada con node --check → OK.
- Creado src/components/pwa/register-sw.tsx ("use client"): RegisterSW con useEffect que SOLO en process.env.NODE_ENV === "production" Y "serviceWorker" in navigator hace navigator.serviceWorker.register("/sw.js").catch(() => {}) (silencioso, nunca rompe la app). Devuelve null.
- Montaje en src/app/layout.tsx: ÚNICAMENTE 2 líneas aditivas — import de RegisterSW y <RegisterSW /> dentro de <body> colocado DESPUÉS del script JSON-LD y ANTES de {children}: renderiza null y registra en useEffect post-hidratación, así que el orden es cosmético (agrupa la infra al inicio del body); metadata, viewport, fonts y JSON-LD quedaron EXACTOS.
- Verificación (dev, localhost:3000):
  - GET /sw.js → 200 (content-type application/javascript), contenido empieza con el header que declara versión ec-offline-v1.
  - GET /manifest.webmanifest → 200 JSON con los 2 iconos (192 y 512).
  - GET /brand/icon-192.png (y -512) → 200.
  - GET / → 200; el HTML YA INCLUYE <link rel="manifest" href="/manifest.webmanifest"/> (Next lo inyecta automáticamente por app/manifest.ts) → NO hizo falta el tercer cambio aditivo en layout.
  - tail dev.log → sin errores nuevos (solo GET / 200). El template "Bail out to client-side rendering: next/dynamic" que asoma en el HTML de dev es el patrón din() preexistente del site-shell, no un error.
  - bun run lint → 0 errores/0 warnings; bunx tsc --noEmit → 0 errores.
- NOTA HONESTA: el SW no se activa en dev (guard de producción en RegisterSW, para no interferir con el hot-reload del dev server) — esto es correcto y esperado. Lo que se verifica en dev es que los archivos se sirvan (sw.js/manifest/iconos) y que el código sea correcto (node --check + lint + tsc). La activación real del SW y el modo offline se validarán en un build de producción.
- Sin tocar: router.ts, navbar, footer, site-shell, page.tsx, globals.css, APIs, schema, vistas. Sin features extra (sin botón de actualización, sin push).

Stage Summary:
- Creados: public/sw.js (service worker offline ec-offline-v1), src/components/pwa/register-sw.tsx. Modificado: src/app/layout.tsx (2 líneas aditivas: import + <RegisterSW />).
- Estrategia elegida: precache de "/" en install (SPA por hash), network-first para navegaciones con fallback al shell y HTML mínimo "Sin conexión" (200), cache-first para /_next/static/ + /brand/ + /images/, /api/* y resto passthrough sin cachear; solo respuestas same-origin basic 200; sin skipWaiting (actualización natural) + clients.claim y limpieza de caches viejas en activate.
- Verificado en dev: sw.js 200 · manifest 200 con 2 iconos · icon-192 200 · GET / 200 con <link rel="manifest"> presente · dev.log limpio · lint 0/0 · tsc 0.
- Pendiente para producción: al hacer build/deploy, verificar en un entorno real que el SW se registra, precachea y que la navegación offline sirve el shell (no verificable con el dev server del sandbox).

---
Task ID: 25-d
Agent: biblioteca-imagenes
Task: 7 imágenes de portada por grupo muscular en la Biblioteca

Work Log:
- Contexto leído: worklog.md (24-b: dataset + BibliotecaView), biblioteca-view.tsx completo y exercises.ts (SOLO tipos/exports: ExerciseGroup = "pecho"|"espalda"|"piernas"|"hombros"|"brazos"|"core"|"full-body" — confirmado, sin modificar el archivo).
- Imágenes generadas con el CLI de la Skill image-generation (`z-ai image`, método CLI directo, size 1152x864 = 4:3 exacto para el wrapper aspect-[4/3]). 7 prompts con familia visual compartida (base común): "Minimalist flat vector fitness illustration, human silhouette athlete, solid dark charcoal background hex 0B0B0E, neon lime green accent hex BDEF27, clean geometric shapes, modern sports app art, no text, no letters, no numbers, no watermark" + movimiento por grupo: pecho=press de banca, espalda=dominadas, piernas=sentadilla, hombros=press militar, brazos=curl de bíceps, core=plancha, full-body=kettlebell swing. 7/7 generadas al primer intento, sin fallas.
- Formato: el CLI escribe extensión .png pero los datos reales son JPEG (verificado con `file`: JFIF 1152x864). Renombré a .jpg (extensión real = nombres exactos pedidos: pecho.jpg … full-body.jpg).
- Integración quirúrgica en biblioteca-view.tsx: import next/image (primer uso de imágenes en el proyecto: no existía patrón <img> ni next/image previo, seguí la indicación de la tarea) + mapa tipado GROUP_IMAGES: Record<ExerciseGroup, string> (7 rutas /images/ejercicios/*.jpg). Cards: wrapper relative -mx-5 -mt-5 aspect-[4/3] overflow-hidden rounded-t-xl border-b (portada full-bleed sobre el contenido), Image fill + object-cover + sizes refinado "(max-width: 640px) 100vw, (max-width: 1024px) 50vw, (max-width: 1280px) 33vw, 25vw" (más preciso que el sugerido porque el grid real es lg:3-cols entre 1024-1280px; cumple igual el lint de next). Alt real: `Ilustración de ejercicios de ${groupLabel(group)}`. Hover: button con clase group + imagen motion-safe:transition-transform motion-safe:duration-300 motion-safe:group-hover:scale-105 (Tailwind v4, respeta prefers-reduced-motion: sin transición ni escala). Dialog: header compacto opcional agregado (aspect-[21/9] rounded-lg, sizes 100vw/512px = max-w-lg del Dialog). NO toqué búsqueda/filtros, EmptyState, IDs, exercises.ts, router, navbar, footer, seo ni APIs.
- NOTA OPERATIVA: al llegar, el dev server NO estaba corriendo (puerto 3000 connection refused, proceso next/bun ausente en ps; dev.log terminaba en un arranque previo). Lo arranqué con el comando canónico del proyecto (bun run dev, puerto 3000, log >> dev.log) — única instancia, para poder cumplir la verificación obligatoria. Revisado ss: solo este proceso en 3000, sin duplicados.
- workspace multi-agente: durante mi lint aparecieron transitoriamente errores en src/components/zona/history-tab.tsx (archivo de otro agente en progreso, no mío); se estabilizaron solos en re-runs. No toqué ese archivo.

Stage Summary:
- Creados: public/images/ejercicios/{pecho,espalda,piernas,hombros,brazos,core,full-body}.jpg — 7 ilustraciones IA flat/vector, paleta carbón #0b0b0e + lima #bdef27, 1152x864, 49-133KB (todas >10KB).
- Modificado: src/components/views/biblioteca-view.tsx (portada en cards + header compacto en dialog; edición puramente visual).
- Verificación: 7 rutas /images/ejercicios/*.jpg → 200 con content-type image/jpeg; GET / → 200; bun run lint → exit 0 (0 errores/0 warnings); bunx tsc --noEmit → exit 0; dev.log sin errores nuevos atribuibles a este cambio (los 4xx/5xx del log son smoke tests de agentes paralelos en /api/zona/*).

---
Task ID: 25-a
Agent: frontend-zona-core
Task: Historial de entrenamientos (5ª tab) + temporizador de descanso en Mi Zona

Work Log:
- Contexto leído: worklog.md (24-c: contratos /api/zona/*; 24-d: patrón de la vista y tabs), api.ts, train-tab.tsx, routines/progress/achievements-tab.tsx, charts.tsx (solo lectura), states.tsx y las rutas sessions GET (lista y detalle) para confirmar los DTO. Verificado EN CÓDIGO: GET /api/zona/sessions?status=todas&limit=20 devuelve SessionDTO[] y GET /api/zona/sessions/[id] devuelve SessionDetailDTO — ambos ya tipados en api.ts y REUSADOS sin cambios (solo edición aditiva, ver abajo). Nota clave: SetDTO/serializeSet NO incluye isPR (isPR solo existe en la respuesta del POST de series); se agregó `isPR?: boolean` opcional al DTO con comentario, y el badge volt "PR" del historial se renderiza solo si el set lo trae (hoy nunca: honesto, sin inventar datos; queda cableado si el backend empieza a enviarlo).
- history-tab.tsx (nuevo, HistoryTab): lista de las últimas 20 sesiones (GET /api/zona/sessions?status=todas&limit=20, fetch en continuación de promesa con flag alive — sin setState síncrono en effects); resumen tipo analytics (3 tiles: sesiones, volumen total, series, calculados SOLO de la lista real) + nota "Mostrando las últimas 20 sesiones" cuando aplica; cards expandibles con aria-expanded/aria-controls (patrón routines-tab): título, fecha con shortDate + hora es-AR, volumen (fmtKg), nº de series, rutina de origen y estado (badge "Completada" / badge volt "Activa" con dot); al expandir pide el detalle y lo cachea en un Map<number, SessionDetailDTO> local (sin refetch; una sesión "activa" siempre refetch por si sumó series); series agrupadas por ejercicio (Map por exerciseId, orden de aparición) con chips "peso × reps", RPE si vino y badge "PR" condicional a isPR; estados: LoadingState, ErrorState con reintentar (lista y detalle por separado), EmptyState honesto "Todavía no tenés entrenamientos registrados" con CTAs "Ir a Entrenar" e "Ir a mis rutinas"; 401 → onActionError → patrón de la casa (AuthGate).
- rest-timer.tsx (nuevo, RestTimer): componente aislado (mismo patrón que SessionTimer: solo él re-renderiza cada segundo), role="timer", setInterval en useEffect con cleanup; countdown mm:ss; botones "−15s"/"+15s" (clamp 15–300, vía ref para no recrear el interval); "Saltar" y cerrar (X); al llegar a 0 anuncia "Descanso terminado" en región aria-live="polite" persistente (anuncio único, nunca cada segundo), muestra el texto 5s y la barra se cierra sola; setState solo dentro de callbacks del interval/timeout (cumple react-hooks v6 set-state-in-effect; verificado en lint).
- train-tab.tsx (integración): tras un POST de serie exitoso arranca el descanso — usa restSec del item planificado de la rutina (block.plannedRest) si existe, si no 90s (DEFAULT_REST_SECONDS), clamp 15–300; estado local {seconds, id} con id incremental: registrar otra serie mientras corre REMONTA el RestTimer (key) → reinicia con el nuevo valor, siempre UN solo timer; chip sticky bottom-4 z-30 al final del flujo (no bloquea formulario ni scroll a 320px; flex-wrap para pantallas chicas, botones min-h-11); al finalizar la sesión el componente se desmonta y el timer desaparece.
- zona-view.tsx (integración): TabId += "historial"; TABS += {Historial, icon History}; grid del tablist sm:grid-cols-5 (en móvil 2 cols, "Historial" col-span-2 sm:col-span-1 para no dejar celda huérfana); render condicional <HistoryTab> solo con la tab activa (patrón existente); onActionError reutilizado; track("zona_tab") existente cubre la analítica — no se agregaron eventos extra.
- Sin tocar: auth-gate.tsx, charts.tsx, routines-tab.tsx, progress-tab.tsx, achievements-tab.tsx, APIs, schema.prisma, router, navbar, footer, site-shell, seo, home-view. Sin datos demo: todo desde las APIs reales (BD la manejan los agentes paralelos; no creé registros).

Stage Summary:
- Creados: src/components/zona/history-tab.tsx, src/components/zona/rest-timer.tsx. Modificados: src/components/zona/api.ts (solo `isPR?: boolean` opcional en SetDTO, aditivo), src/components/zona/train-tab.tsx (rest timer tras registrar serie), src/components/views/zona-view.tsx (5ª tab Historial).
- Cómo funciona: Historial lista/completa las últimas 20 sesiones con resumen agregado y detalle por ejercicio cacheado por sesión (refetch automático de la sesión activa); RestTimer arranca tras cada serie registrada con el descanso planificado de la rutina (o 90s), ajustable ±15s (15–300), con countdown mm:ss accesible, anuncio "Descanso terminado" al llegar a 0 y cierre manual o automático; un único timer a la vez que se reinicia con cada nueva serie.
- Verificación: bun run lint → 0 errores/0 warnings; bunx tsc --noEmit → 0 errores; GET / → 200; dev.log con "✓ Compiled" y sin errores nuevos de compilación (los 4xx del log son smoke tests de agentes paralelos). NO inicié ni reinicié el dev server, no toqué el build.

---
Task ID: 25-b
Agent: backend-zona-extra
Task: Plan semanal + export de datos + última performance (backend)

Work Log:
- Leídos worklog.md (24-c: patrones zona-auth/zona-utils/serializers/rate-limit/aislamiento), prisma/schema.prisma, src/lib/zona-auth.ts, zona-utils.ts, rate-limit.ts, content/exercises.ts (exports verificados: EXERCISES, getExerciseById, exerciseName) y rutas de ejemplo (routines, weight, sessions/[id]/sets, sessions, sessions/[id]/finish, profile, habits).
- A) Modelo aditivo WeekSlot en prisma/schema.prisma (id String cuid, profileId Int, weekday Int 0=Lunes..6=Domingo, routineId Int? con onDelete SetNull, day Int?, createdAt, @@unique([profileId, weekday]) + @@index([profileId])) + relaciones inversas aditivas: ClientProfile.weekSlots WeekSlot[] y Routine.weekSlots WeekSlot[]. Nada existente modificado. Nota: el enunciado decía profileId String, pero ClientProfile.id es Int autoincrement en este proyecto → se tipó Int para que la relación compile. bun run db:push OK (db/custom.db) y Prisma Client regenerado.
- B1) src/app/api/zona/schedule/route.ts (nuevo): GET devuelve SIEMPRE array de 7 (weekday 0..6, cada uno {weekday, routineId, routineTitle, day} con nulls si no hay asignación; routineTitle resuelto vía include routine.select.name). PUT con zod {slots:[{weekday 0..6, routineId Int|null, day 1..6|null}]} + rate-limit suave 20/min (clientKey "zona-schedule", 429 con Retry-After); validaciones: duplicados de weekday → 400, day sin routineId → 400 "El día solo puede indicarse junto a una rutina.", rutina no propia → 404 "Rutina no encontrada.", rutina inactiva → 400 "La rutina no está activa.", day > daysPerWeek → 400 con nombre de rutina; reemplazo completo en $transaction (deleteMany profileId + createMany máx 7) y 200 con la misma serialización del GET. Owner SIEMPRE por profileId de la sesión.
- B2) src/app/api/zona/export/route.ts (nuevo): GET {exportedAt, profile:{id,name,createdAt}, routines:[serializeRoutine con exerciseName], sessions:[serializeSession + sets:[serializeSet]], weights:[{date,kg}], water:[{date,ml}], habits:[{date,habit,done}]}; take máx 500 por colección; headers Content-Type application/json + Content-Disposition: attachment; filename="ezequiel-coaching-mis-datos.json". Desviación honesta del enunciado: ClientProfile NO tiene goal/level/equipment/daysPerWeek (viven en Routine) → profile solo {id,name,createdAt}; PRs no están persistidos en ninguna tabla (se calculan en finish) → se exportan sets + volumeKg reales sin columnas inventadas. JAMÁS pinHash/sessionToken/sessionExpiry.
- B3) src/app/api/zona/last-performance/route.ts (nuevo): GET ?exerciseId= → 400 si no existe en biblioteca (getExerciseById); busca SetLog del ejercicio en sesiones status "completada" del perfil (session.profileId de la sesión, nunca del body), orderBy createdAt desc + id desc, take 500, agrupa una serie por sesión (la más reciente de cada una) y ordena sesiones por finishedAt desc; responde 200 {exerciseId, last:{weightKg, reps, date}|null, previous:{...}|null} donde date = finishedAt ISO de la sesión. Sin datos → {exerciseId, last:null, previous:null}. HONESTIDAD: solo datos reales, sin "sugerencias".
- Smoke test E2E con curl + cookie jars (/tmp/jar1.txt, /tmp/jar2.txt), 10/10 OK:
  1) Registro "SmokeVeinticincoB" → 201 {id:9,name} + cookie.
  2) Rutina manual 3 días (press-banca/sentadilla-barra/peso-muerto) → 201 id:5.
  3) PUT schedule 3 slots (0/2/4 con días 1/2/3) → 200 con los 7 días serializados; GET idéntico (otros 4 días null).
  4) PUT routineId de OTRO perfil (id 6) → 404 "Rutina no encontrada."; weekday=9 → 400 "El día de la semana va de 0 (lunes) a 6 (domingo)."; day=7 → 400 (zod max 6) y day=4 > daysPerWeek=3 → 400 "La rutina \"Full Body 25B\" tiene 3 días por semana."; extra: day sin routineId → 400.
  5) POST /sessions {routineId:5, day:1} → 201 "Full Body 25B · Día 1" (id:5); 2 series press-banca (60kg×8 → isPR previousMax:null; 62.5kg×8 → volumen 980); finish → 200 status completada, newPRs [{62.5, 8, e1rm 79.2}].
  6) GET last-performance?exerciseId=press-banca con 1 sesión → 200 {last:{62.5,8,fecha finish}, previous:null}; ejercicio inexistente → 400; tras 2ª sesión (60kg×8) + finish → {last:{60,8,...}, previous:{62.5,8,fecha 1ª sesión}} ✓ tendencia correcta.
  7) GET export → 200, headers exactos (attachment, filename correcto), JSON parseable con 1 rutina/3 items, 2 sesiones con sets y volumen (980 y 480), weights [{78.5}], water [{500}], habits [{sueno,true}]; grep pinHash|sessionToken|sessionExpiry → 0 ocurrencias.
  8) Sin cookie: schedule/export/last-performance → 401 "Iniciá sesión para continuar." (los 3).
  9) AISLAMIENTO: 2º perfil "SmokeAjenoB" (id:10) → su schedule son 7 días null, su last-performance {last:null,previous:null} y su export solo contiene SU rutina (1) y cero sesiones/logs del 1º.
  10) Limpieza con script bun+PrismaClient (scripts/_cleanup-25b.tmp.ts, borrado tras uso): conteo ANTES/DESPUÉS de las 9 tablas (las 8 de zona + WeekSlot) → todo en 0. Nota operativa: un proceso concurrente (agente 25-a, testeando en paralelo) ya había vaciado los ClientProfile a las 01:08; mi script verificó el estado final con deleteMany idempotente y conteo explícito de las 9 tablas en 0. La BD se entrega VACÍA.
- INCIDENTE OPERATIVO (mismo patrón que 24-c): tras db:push, el dev server en memoria tenía el PrismaClient anterior → db.weekSlot undefined (500 en schedule). El cliente regenerado en disco SÍ exponía weekSlot (verificado con script). Espera + reintentos no bastaron; reinicié el dev server (única instancia, mismo comando bun run dev, puerto 3000, log en dev.log) — imprescindible para los smoke tests obligatorios. Un intento intermedio murió por EADDRINUSE transitorio al solaparse con el socket previo; la instancia final quedó estable.
- Verificación final: bun run lint → 0/0; bunx tsc --noEmit → 0; curl GET / → 200; dev.log sin errores nuevos atribuibles a mis rutas (los errores de hidratación posteriores provienen del E2E browser del agente 25-a en paralelo; mis cambios son solo API + schema).

Stage Summary:
- Creados: prisma modelo WeekSlot (+2 relaciones inversas aditivas) y 3 route.ts: src/app/api/zona/schedule/route.ts, src/app/api/zona/export/route.ts, src/app/api/zona/last-performance/route.ts. Ningún archivo existente modificado (schema solo aditivo).
- Contratos para el frontend (Task 25-frontend): GET/PUT /api/zona/schedule → array fijo de 7 slots {weekday, routineId: number|null, routineTitle: string|null, day: number|null} (routineId es number, no string: las rutinas usan ids autoincrement); GET /api/zona/export → descarga JSON (no parsear UI salvo para vista previa); GET /api/zona/last-performance?exerciseId= → {last, previous} nullables para hint de progresión (ocultar si last null).
- Seguridad/aislamiento intactos: owner por profileId de la cookie en todas las queries, 404 sin revelar existencia, export sin campos sensibles, rate-limit suave en PUT schedule.
- lint 0 · tsc 0 · GET / 200 · BD entregada VACÍA (9 tablas en 0, WeekSlot incluido).

---
Task ID: 25-e
Agent: frontend-zona-extra
Task: Plan semanal + exportar datos + hint última vez (frontend)

Work Log:
- Contexto leído: worklog.md (25-b: contratos EXACTOS de schedule/export/last-performance verificados con smoke tests; 25-a: rest-timer + historial recién integrados; 24-d: patrones de la vista), api.ts, routines-tab.tsx, progress-tab.tsx, train-tab.tsx, zona-view.tsx, rest-timer.tsx + history-tab.tsx (solo lectura), ui/dialog + ui/input + states, schedule/last-performance route.ts y analytics.ts.
- api.ts (aditivo): tipos ScheduleSlotDTO (array fijo de 7, weekday 0=Lunes..6=Domingo), PerformancePointDTO y LastPerformanceDTO ({last, previous} nullables). Sin tocar nada existente.
- FEATURE 1 — Plan semanal en routines-tab.tsx: nuevo componente WeeklyPlan colocado ARRIBA del listado (jerarquía: primero la semana, después las rutinas; con 0 rutinas la sección NO se monta — no hay nada que asignar y los anclajes EmptyState + generador IA quedan intactos). Fila de 7 días con chips "Título rutina · Día N" (botón editar con aria-label completo) + X "Quitar la asignación del X" (botones hermanos, nunca anidados), días libres con "+ Asignar" (min-h-11). Dialog (ui/dialog) con select de rutina (solo activas: el API rechaza inactivas) + select de día 1..daysPerWeek según la rutina elegida + Guardar (disabled hasta elegir, spinner "Guardando…") + "Quitar asignación" cuando el día ya está ocupado. Guardado: PUT del array COMPLETO de 7 (libres como {weekday, routineId:null, day:null}), setState con la respuesta 200 del propio PUT (viene re-leída de BD), toast + track("zona_schedule_save", {weekdays:n}); errores → onActionError (401 → AuthGate, resto toast) y el diálogo queda abierto para reintentar. GET de schedule en efecto con setState SOLO en continuaciones .then/.catch/finally (react-hooks v6), deps [routines, onActionError] (tras borrar/crear rutina el plan se reconcilia), flag alive contra desmontaje. Loading inline con spinner + ErrorState con Reintentar. Responsive a 320px: DECISIÓN DOCUMENTADA — fila scrolleable horizontal (li w-28 shrink-0 snap-start + overflow-x-auto + hint "Deslizá la fila para ver los 7 días") en vez de grid 7 columnas: a 320px cada columna tendría ~40px y no entran "Título · Día N" + X; en ≥sm pasa a grid sm:grid-cols-7. Encontré y ARREGLÉ un quirk de Chromium: el scroller inflaba documentElement.scrollWidth (813px) aunque la página NO scrolleaba; verifiqué con eval (remove/restore del ul + probes de estilos) y lo cerré con contain-paint (html.scrollWidth volvió a 320 exacto; en sm:contain-none para no cortar el grid).
- FEATURE 2 — Exportar en progress-tab.tsx: ExportCard discreta AL FONDO del tab (después de la tabla de PRs): "Tus datos son tuyos" + botón outline "Exportar mis datos" (icono Download, el tab ya usaba lucide). Flujo: fetch GET /api/zona/export con credentials same-origin (fetch directo: zonaApi parsea JSON y el export hay que bajarlo como blob) → !res.ok parsea {error} verbatim, 401 → throw ZonaUnauthorized → onActionError → AuthGate → blob → URL.createObjectURL → <a download="ezequiel-coaching-mis-datos.json"> click programático → revocar URL → toast "Descarga lista" + track("zona_export"). Estado "Exportando…" con spinner en el botón (disabled); errores de red con el mensaje honesto del patrón zonaApi.
- FEATURE 3 — Hint "Última vez" en train-tab.tsx: LastPerformanceHint renderizado dentro de la celda del input de peso de CADA bloque de ejercicio (cubre planificados y añadidos en libre — el select del picker crea el bloque y el hint aparece con él). Cache en React.useRef<Map<string, LastPerformanceDTO>> del TrainTab (vive mientras dura la sesión activa); estado inicial se hidrata desde el cache (sin parpadeo ni refetch), el effect resuelve cache-hit vía Promise.resolve (setState SOLO en continuaciones) o GET /api/zona/last-performance?exerciseId=; flag alive en cleanup ignora respuestas obsoletas si el ejercicio cambia rápido; errores de red SILENCIOSOS (hint es best-effort, no rompe el registro de series), 401 → onActionError. Render solo si last != null && last.weightKg != null (sin registros con peso → estado limpio): chip-botón "Última vez: 62,5 kg × 8 · 12 mar" (fmtKg + shortDate con date.slice(0,10) — el API manda ISO completo y shortDate espera yyyy-mm-dd), aria-label "Usar último peso registrado", click → rellena el input de peso; si previous.weightKg > last.weightKg añade "↓ bajó X kg vs. la anterior" (dato real, sin sugerencias). min-h-11 sm:min-h-9 como los sm buttons del proyecto. REST-TIMER DE 25-a Y LÓGICA DE SERIES/PRs PRESERVADOS INTACTOS (diff puramente aditivo: 1 ref, 1 componente nuevo, 1 render en la celda de peso).
- E2E en navegador (agent-browser, cuenta smoke vía API + limpieza): login → Plan semanal pinta el GET (Lun/Mié asignados), asignar Jueves vía diálogo persiste (tras reload sigue), X quita Jueves, aria-labels correctos en listitems/botones; "Entrenar día 1" → serie 60kg×8 (rest timer de 25-a operativo) → finalizar → nueva sesión muestra hint "Última vez: 60 kg × 8 · 10 sept" y al tocarlo el input queda en "60"; Progreso → export → GET 200 + toast "Descarga lista"; localStorage: zona_schedule_save {weekdays:3}/{weekdays:2} y zona_export presentes; consola sin errores. Limpieza con PrismaClient deleteMany (9 tablas) → conteo final TODO en 0; script temporal borrado.

Stage Summary:
- Modificados: src/components/zona/api.ts (3 DTOs aditivos), src/components/zona/routines-tab.tsx (WeeklyPlan + wiring), src/components/zona/progress-tab.tsx (ExportCard), src/components/zona/train-tab.tsx (LastPerformanceHint). Sin cambios en zona-view, rest-timer, history-tab, auth-gate, charts, achievements-tab ni ninguna API route/schema.
- Cómo funciona: Rutinas muestra arriba el Plan semanal — tocás un día libre → dialog (rutina + día de la rutina) → Guardar hace PUT del array de 7 y repinta; chip con "Título · Día N" editable y X que quita al toque; mobile scrollea la fila (sin overflow de página a 320px). Progreso tiene "Exportar mis datos" al fondo: descarga ezequiel-coaching-mis-datos.json con TODO el perfil. En Entrenar, cada bloque de ejercicio muestra "Última vez: X kg × Y · fecha" (cacheado por sesión activa, respuesta obsoleta ignorada) que rellena el peso con un toque, y "↓ bajó X kg" solo si la sesión anterior fue más pesada — nunca sugerencias inventadas.
- Verificación: bun run lint → 0 errores/0 warnings; bunx tsc --noEmit → 0; curl GET / → 200; los 3 endpoints nuevos → 401 "Iniciá sesión para continuar." sin cookie (esperado); dev.log sin errores nuevos atribuibles a este cambio (los de hidratación de navbar son preexistentes de un E2E de otro agente, archivo que no toqué). NO inicié ni reinicié el dev server, no toqué el build. BD entregada VACÍA (9 tablas de zona en 0, verificado con conteo post-deleteMany).

---
Task ID: 25
Agent: main (Z.ai Code)
Task: Integración de features restantes del wishlist de 20 proyectos fitness + verificación E2E final consolidada

Work Log:
- Auditoría previa (solo lectura): mapeados los 20 proyectos contra el estado real (Tasks 24-b/c/d/e ya cubrían biblioteca 100 ejercicios, rutinas + IA, sesiones con PRs, peso/agua/hábitos, logros, analytics). Faltaban: historial de entrenamientos, rest timer, scheduling semanal, export de datos, hint "última vez", PWA offline, imágenes de biblioteca.
- Lote paralelo: 25-a (historial + rest timer, frontend), 25-b (backend: modelo WeekSlot aditivo + /api/zona/schedule + /api/zona/export + /api/zona/last-performance; 10/10 smoke tests; BD → 0), 25-c (sw.js ec-offline-v1 + RegisterSW prod-only + 2 líneas en layout; manifest ya existía y Next inyecta el link), 25-d (7 imágenes IA coherentes en /images/ejercicios/ + covers en biblioteca-view con next/image).
- 25-e secuencial (tras 25-a/b): WeeklyPlan en routines-tab (PUT array completo, Dialog asignar, scroller horizontal a 320px con contain-paint), ExportCard en progress-tab (blob + download programático), LastPerformanceHint en train-tab (cache Map, respuestas obsoletas ignoradas, click rellena peso; solo datos reales, sin pseudo-ciencia).
- E2E main con agent-browser: registro/login → 5 tabs; plan semanal: asignar Lunes→"E2E Full Body, día 1" persistido y editable; sesión día 1: serie 60×8 → badge PR + toast + REST TIMER 1:29 (tomó restSec=90 de la rutina) con −15s/+15s/Saltar; serie remo 60×10 → PR; finish → resumen 1.080 kg/2 series/0:51 + récords e1RM Epley exactos (76/80 kg); Historial: tiles (1/1.080/2) + card Completada expandible con series por ejercicio; Progreso: tabla PRs + export descarga JSON real (verificado contenido: profile/routines/sessions sin campos sensibles) + toast "Descarga lista"; 2ª sesión → hint "Última vez: 60 kg × 8 · 10 sept" en ambos ejercicios, click rellena el input (verificado value=60); export y descargas de prueba borradas.
- Responsive: 320px sw==320==cw en Mi Zona y Biblioteca (imágenes fluidas); viewport desktop OK.
- BD de prueba eliminada con cascada → 9 tablas en 0 (sin datos demo).
- Endpoints finales: / 200 · /manifest.webmanifest 200 · /sw.js 200 · iconos 200 · imágenes 200 · /api/zona/schedule 401 sin sesión (aislamiento OK).
- NOTA: error de hidratación en dev.log es PREEXISTENTE y ambiental (navegador headless con prefers-reduced-motion: reduce vs SSR sin él — warning motion.dev justo antes en el log; ya presente en dev.log antes de tocar nada esta sesión). React regenera el árbol; sin impacto en usuario real. Pendiente para un futuro bloque de "hidratación segura" si se quiere (CONSERVAR > CAMBIAR).

Stage Summary:
- Features nuevas integradas y verificadas E2E: Historial (tab 5), Rest timer, Plan semanal, Export JSON, Hint "última vez", PWA offline (SW prod), 7 imágenes de biblioteca.
- lint 0/0 · tsc 0 · BD vacía · sin push (el repo remoto quedó en Task 23; push solo a pedido del usuario).
- Wishlist de los 20 proyectos: CUBIERTA salvo no-aplicables (Firebase → Prisma/SQLite local, pagos → solo reporte, reemplazos de sistemas que funcionan).
---
Task ID: 26-a
Agent: backend-zona-v2
Task: Editar/duplicar rutinas + metas + heatmap + 3 logros (backend)

Work Log:
- Contexto leído: worklog.md (24-c: patrones zona-auth/zona-utils/rate-limit/owner-por-sesión; 25-b: schema aditivo WeekSlot + smoke tests con cookie jars; 25: estado consolidado), prisma/schema.prisma, zona-auth.ts, zona-utils.ts y las rutas routines, routines/[id], progress, achievements, profile, export, schedule.
- A) SCHEMA ADITIVO: ClientProfile += weightGoalKg Float? y waterGoalMl Int? (null = sin meta). bun run db:push OK (db/custom.db) + Prisma Client regenerado. Nada destructivo.
- B1) PATCH /api/zona/routines/[id] (en [id]/route.ts, GET/DELETE intactos): zod espejo del create (name 2-60, enums GOALS/LEVELS/EQUIPMENT, daysPerWeek 1-6, items min 1 máx 120; constantes duplicadas en el archivo a propósito — Next no permite exports extra en route.ts). Body con los 6 campos opcionales; sin ninguno → 400 "Enviá al menos un campo para editar.". Owner check findFirst {id, profileId de sesión, active:true} → 404 también para archivadas (no se editan). Si items: exerciseIds validados contra biblioteca (400 listando inválidos) y day ≤ daysPerWeek EFECTIVO (el nuevo si viene en el mismo body). GUARD EXTRA documentado: si se baja daysPerWeek SIN items y existen items con day > nuevo → 400 "Hay ejercicios existentes asignados a un día mayor que el nuevo daysPerWeek..." (evita dejar items huérfanos fuera de rango). Aplicación: $transaction (deleteMany items + createMany con orderIdx por día — mismo patrón que create) + update de escalares. 200 → serializeRoutine completo.
- B2) POST /api/zona/routines/[id]/duplicate (nuevo archivo): rate-limit 20/min por IP (clientKey "zona-duplicate", 429 + Retry-After), owner check igual que GET/DELETE (permite duplicar archivadas: la copia sale active:true), copia name = "<name> (copia)".slice(0,60), mismos goal/level/daysPerWeek/equipment/notes/source, items copiados con mismos days/orderIdx (nested create). 201 → serializeRoutine.
- B3) PATCH /api/zona/profile (en profile/route.ts, GET/POST intactos): zod {weightGoalKg 30-300 o null, waterGoalMl int 500-5000 o null, ambos optional}; sin ningún campo → 400 "Enviá al menos una meta (weightGoalKg o waterGoalMl).". db.clientProfile.update SOLO de los campos presentes, perfil SIEMPRE el de la sesión → 200 {id, name, weightGoalKg, waterGoalMl}. GET aditivo: {...publicProfile, weightGoalKg, waterGoalMl}. Export: profile += weightGoalKg/waterGoalMl (aditivo).
- B4) GET /api/zona/progress: nuevo campo activityDays: string[] (YYYY-MM-DD UTC) con CUALQUIER actividad (misma regla del streak: serie registrada, peso, agua ≥500 ml, hábito done) en los últimos 90 días (hoy incluido; ventana = dateKey(since90) con since90 = addDays(todayUtc,-89) ya existente). CERO queries nuevas: filtra por string-compare el Set<string> activityDays que el endpoint ya construía para la racha (fuentes: setLog.createdAt + weight/water/habit date) y lo ordena asc. Todos los campos previos intactos (activityDays va entre streak y activeSession).
- B5) GET /api/zona/achievements: 13 definiciones (antes 10). DECISIÓN DE ORDEN: cada logro nuevo se inserta INMEDIATAMENTE después de su hermano menor — orden final: primera-sesion, racha-7, racha-14*, sesiones-10, sesiones-25, sesiones-50*, primer-pr, prs-5, volumen-10t, volumen-50t*, agua-7, peso-4, rutina-ia. sesiones-50 {unlocked: completadas≥50, progress current/50}; volumen-50t {unlocked: ≥50.000 kg, progress Math.round(kg)/50000}; racha-14 {unlocked: streak≥14, progress min(streak,14)/14} — incluye progress (patrón agua-7) aunque racha-7 no lo tenga; racha-7 se dejó intacto. Desbloqueo 100% real, sin datos.
- Smoke tests curl + cookie jars (/tmp/jar1.txt, /tmp/jar2.txt) — TODOS OK:
  1) 401 sin cookie en los 4 endpoints nuevos/modify (PATCH routine, POST duplicate, PATCH profile, GET progress) → "Iniciá sesión para continuar." ✓ (verificado antes y después del reinicio del server).
  2) Registro SmokeVeintiseisA (201) + rutina 3 días/4 items (201, id 9) → PATCH {name, daysPerWeek 3→2, items nuevos} 200 con items reemplazados (ids nuevos, orderIdx por día, notes conservado) y GET refleja todo; PATCH exerciseId inválido → 400 "Ejercicios no encontrados en la biblioteca: ejercicio-fantasma."; PATCH items day 5 con dpw 4 → 400; PATCH dpw=2+items day 3 juntos → 400 (valida contra el NUEVO dpw); PATCH solo dpw=2 con item existente en day 3 → 400 (guard extra); PATCH {} → 400; PATCH rutina archivada (DELETE previo) → 404; PATCH + duplicate de rutina AJENA (2º perfil SmokeAjenoVeintiseis) → 404 "Rutina no encontrada." (ambos).
  3) POST duplicate → 201 {id:10, name:"Upper Lower 26A (copia)", 3 items con mismos days/orderIdx}; editar el original (name+items) NO tocó la copia (GET verificado) — copia independiente.
  4) PATCH profile {weightGoalKg:82.5, waterGoalMl:2500} → 200 exacto {id,name,weightGoalKg,waterGoalMl}; GET → mismos valores; waterGoalMl:null → 200 limpia solo agua conservando 82.5; fuera de rango: 25 → 400, 6000 → 400, 1500.5 → 400 "entero", {} → 400.
  5) Sesión desde rutina 9 día 1 (201 "Solo Tren Superior · Día 1") + set 60kg×8 (201 isPR:true previousMax:null) + agua 600 + peso 80.5 + hábito sueno → GET progress: activityDays ["2026-09-10"], streak {days:1}, TODAS las claves previas presentes; perfil ajeno sin actividad → activityDays []. finish → 200; sesión 2 libre con set 60×8 → isPR, volumen 480, finish newPRs e1rm 76.
  6) Logros (datos reales): 13 defs en el orden documentado; primera-sesion unlocked:true y primer-pr unlocked:true tras sesión+PR reales; sesiones-50 progress 2/50; racha-14 progress 1/14; volumen-50t progress 480/50000 (60×8=480 exacto); perfil sin datos → 13 defs, 0 unlocked.
  7) GET export → 200: profile incluye weightGoalKg:82.5 y waterGoalMl:2500; grep pinHash/sessionToken/sessionExpiry → 0 ocurrencias; 2 rutinas, 2 sesiones con sets, weights/water/habits reales.
- INCIDENTE OPERATIVO (mismo patrón que 24-c/25-b): tras db:push, el dev server en memoria tenía el PrismaClient viejo → PATCH profile devolvía 500 "Unknown argument `weightGoalKg`" (el código nuevo SÍ estaba hot-reloadado; el delegado viejo no conocía la columna). Espera +5s y touch de node_modules/.prisma/client NO bastaron. Se siguió el procedimiento documentado: reinicio del ÚNICO dev server (mismo comando bun run dev, puerto 3000, log dev.log). NOTA NUEVA de esta sesión: la plataforma mata procesos background spawneados por un comando del agente cuando ese comando termina (el server muere entre comandos aunque sea setsid+nohup) → los smoke tests se ejecutaron en 3 comandos "auto-contenidos" que arrancan el server, esperan ready, corren su parte y terminan. Al cierre el server quedó levantado (home 200) pero puede requerir que el orquestador lo relance si el sandbox lo recolecta.
- LIMPIEZA FINAL: script bun+PrismaClient (scripts/tmp-26a/cleanup-26a.ts, borrado tras uso) → ANTES {profile 2, routine 2, routineItem 4, workoutSession 2, setLog 1, weightLog 1, waterLog 1, habitLog 1, weekSlot 0} → DESPUÉS deleteMany ClientProfile en cascada → 9 tablas de zona en 0 (conteo explícito). Cero datos demo.
- Verificación: bunx tsc --noEmit → 0 errores (proyecto completo). Lint de MIS archivos (6 route.ts) → 0 errores. Lint del PROYECTO COMPLETO → 1 error PREEXISTENTE/FOREAJO de esta sesión: src/components/site/footer.tsx:64 react-hooks/set-state-in-effect, modificado en paralelo por otro agente (02:25, git status lo atribuye a un cambio que no es de 26-a; mi alcance prohibía tocar src/components) → no lo toqué para no pisar al agente paralelo. curl GET / → 200.

Stage Summary:
- Archivos: MODIFICADOS prisma/schema.prisma (weightGoalKg/waterGoalMl), src/app/api/zona/routines/[id]/route.ts (+PATCH, GET/DELETE intactos), src/app/api/zona/profile/route.ts (+PATCH, GET aditivo, POST intacto), src/app/api/zona/progress/route.ts (+activityDays, cero queries extra), src/app/api/zona/achievements/route.ts (10→13 logros), src/app/api/zona/export/route.ts (profile += metas). NUEVO: src/app/api/zona/routines/[id]/duplicate/route.ts.
- Shapes nuevas EXACTAS:
  - PATCH /api/zona/routines/[id] → 200 serializeRoutine completo {id,name,goal,level,daysPerWeek,equipment,source,notes,active,createdAt,items:[{id,day,orderIdx,exerciseId,exerciseName,sets,reps,restSec,notes}]}; errores: 400 (zod/sin campos/ids inválidos/day overflow/dpw-reduce guard), 404 (ajena/inexistente/archivada), 401, 500.
  - POST /api/zona/routines/[id]/duplicate → 201 misma shape de rutina (copia independiente, name "(copia)" máx 60); 429 con Retry-After a >20/min; 404 ajena.
  - PATCH /api/zona/profile → 200 {id:number, name:string, weightGoalKg:number|null, waterGoalMl:number|null}; 400 zod o body sin campos; GET profile ahora {id,name,weightGoalKg,waterGoalMl} (aditivo).
  - GET /api/zona/progress → campo NUEVO activityDays: string[] ("YYYY-MM-DD" UTC asc, ≤90 días, hoy incluido, criterio streak) + TODOS los campos previos (weights, waterToday, waterWeek, habitsToday, habitsWeek, volumeByWeek, prs, totals, streak, activeSession).
  - GET /api/zona/achievements → 13 items {id,title,desc,unlocked,progress?}; nuevos: racha-14 (tras racha-7, progress d/14), sesiones-50 (tras sesiones-25, progress x/50), volumen-50t (tras volumen-10t, progress kg/50000).
  - GET /api/zona/export → profile ahora {id,name,createdAt,weightGoalKg,waterGoalMl} (aditivo).
- BD entregada VACÍA: 9 tablas de zona en 0 (verificadas con conteo explícito post-cascada).
- Nota frontend (Task 26-b/26-c): metas nullable (sin meta = null, UI decidir placeholder); el heatmap puede pintar directo activityDays; achievements-tab ya pinta progressbar si progress viene (racha-14 lo trae); para editar rutinas usar PATCH parcial (solo campos que cambian) y duplicate POST para copiar.

---
Task ID: 26-c
Agent: biblioteca-v2
Task: +50 ejercicios reales y filtro por músculo primario

Work Log:
- Contexto leído: worklog.md (24-b: contrato EXACTO de exercises.ts — primaryMuscles ≤2, secondaryMuscles ≤3, 4-6 pasos, 1-2 tips, ids kebab-case únicos, ids INMUTABLES; 25-d: imágenes por grupo ya integradas), exercises.ts COMPLETO (100 ids + helper filterExercises + vocabulario de músculos), biblioteca-view.tsx, api/zona/routines/generate/route.ts (solo lectura — el generador IA consume EXERCISES.slice(0,60) y quedó intacto).
- A) +50 EJERCICIOS (100 → 150): dedupe previo contra los 100 ids existentes ANTES de escribir (muchos candidatos sugeridos ya existían: jalon-polea, prensa-piernas, sentadilla-bulgara, hip-thrust, face-pull, remo-invertido, curl-martillo, mountain-climbers, farmer-walk, etc. → descartados). Solo clásicos universalmente documentados, nombres en español neutro. Entradas AGREGADAS AL FINAL de cada array de grupo con comentario marcador "Task 26-c" (el array EXERCISES y sus spreads quedaron intactos; módulo sigue isomórfico, sin "use client"). Músculos: REUSO 100% del vocabulario existente (0 strings nuevos, verificado por script contra el set del dataset original). Reparto: pecho +6 (press declinado mancuernas, press inclinado máquina, aperturas máquina pec deck, aperturas banda, flexiones inclinadas/declinadas), espalda +7 (dominada agarre neutro, jalón supino, remo banco inclinado, remo banda sentado, buenos días, superman, encogimientos barra), piernas +10 (zancadas caminando/lateral/con salto, sentadilla salto/isométrica pared/pistol, peso muerto a una pierna, puente glúteo unilateral, aducción máquina, curl nórdico), hombros +6 (press mancuernas de pie, press sentado barra, elevación lateral polea, press hombros banda, press vertical/handstand push-up, face pull banda), brazos +7 (curl Scott barra, curl invertido, curl banda, extensión tríceps banda, curl/extensión de muñecas, dead hang), core +8 (sit-up, crunch invertido, v-ups, tijeras verticales, toes to bar, leñador polea, flexión lateral tronco, l-sit), full-body +6 (arranque mancuernas, clean mancuernas, clean barra, gateo oso, suitcase carry, salto horizontal). Nivel: 24 principiante / 19 intermedio / 7 avanzado. Equipamiento: los 7 tipos del enum siguen cubiertos (banda y kettlebell reforzados con 6 y 4 entradas nuevas). Wall ball descartado: el enum ExerciseEquipment no tiene "balón medicinal" y expandir el enum rompería contratos de zona.
- VALIDACIÓN OBLIGATORIA (script temporal bun en /tmp/validate-26c.ts, BORRADO al final): importa el dataset ACTUAL y el de git HEAD (git show → módulo TS), y verifica: (1) 100+50=150 exacto; (2) 0 ids duplicados y 0 nombres duplicados; (3) las 100 entradas de HEAD intactas (JSON profundo idéntico por id) y presentes; (4) contracto en las 150: primaryMuscles 1-2, secondaryMuscles ≤3, instructions 4-6, tips 1-2, group/equipment/level dentro de enums, sin strings vacíos; (5) ids nuevos kebab-case; (6) 0 strings de músculo nuevos; (7) los 7 grupos cubiertos. OUTPUT: "HEAD: 100 ejercicios | ACTUAL: 150 ejercicios | +50 … OK - 0 problemas: sin ids duplicados, entradas originales intactas, contrato cumplido, todos los grupos cubiertos." git diff exercises.ts = +1008/-27: SOLO adiciones dentro de los arrays + 2 líneas del doc-comment de cabecera (~100→~150); las -25 restantes corresponden a un REORDENAMIENTO de 2 entradas existentes (remo-menton, flexiones-pike) hecho en paralelo por otro agente sobre el mismo archivo (mis cambios no las tocaron; la inmutabilidad quedó demostrada por deep-compare contra HEAD, no por line-diff).
- B) FILTRO POR MÚSCULO PRIMARIO en biblioteca-view.tsx: 4º select "Músculo primario" junto a grupo/equipo/nivel. Opciones derivadas en useMemo de los primaryMuscles REALES del dataset (unique + sort localeCompare "es", 29 opciones hoy — NUNCA hardcodeado, escala si el dataset crece). Integración ADITIVA sin tocar exercises.ts: results useMemo filtra encima de filterExercises (músculo === ANY_MUSCLE ? base : base.filter(primaryMuscles.includes)). hasFilters incluye músculo; "Limpiar filtros" lo resetea; track("biblioteca_filter") extendió su payload con muscle (mismo patrón next ?? state); contador aria-live sigue contando results.length (funciona con 4 filtros). Chips de músculos en cards y dialog intactos (usan exercise.primaryMuscles/secondaryMuscles directos). Descripción fija actualizada: "Más de 150 ejercicios… Filtra por grupo muscular, músculo, material disponible o nivel" (el badge {EXERCISES.length} ya era dinámico → "150 ejercicios · 7 grupos"). RESPONSIVE: grid de filtros pasado a grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 con buscador sm:col-span-2 (sm: buscador solo + grupo/equipo + nivel/músculo; lg: buscador+grupo / equipo/nivel/músculo; a 320px columna única — sin celdas vacías en ningún breakpoint). NO tocados: GROUP_IMAGES (los nuevos heredan la portada por grupo), navbar/footer/router/seo/APIs, generate/route.ts.
- C) VERIFICACIÓN: bunx tsc --noEmit → 0 errores. Lint de MIS archivos (exercises.ts + biblioteca-view.tsx) → 0 errores/0 warnings. bun run lint (proyecto completo) → 1 error FOREAJO preexistente en esta sesión: src/components/site/footer.tsx:64 react-hooks/set-state-in-effect — cambio concurrente no commiteado de otro agente (no existe en git HEAD: verificado con git show; también documentado por el agente de 26-a); footer está en mi lista de NO-TOCAR → no lo toqué para no pisar el trabajo en curso. curl GET / → 200; POST /api/zona/routines/generate sin cookie → 401 (el módulo del generador carga y consume el dataset ampliado sin romper); dev.log solo con "✓ Compiled" y requests 200/401/201 — sin errores de compilación. Dev server NO reiniciado.

Stage Summary:
- Total ejercicios: 150 (12→18 pecho, 16→23 espalda, 20→30 piernas, 12→18 hombros, 14→21 brazos, 14→22 core, 12→18 full-body). 0 ids duplicados, 100 ids originales intactos byte a byte, contrato 24-b cumplido en las 150 entradas, 29 opciones de músculo derivadas del dataset.
- Archivos tocados: src/lib/content/exercises.ts (solo adiciones + doc-comment) y src/components/views/biblioteca-view.tsx (4º filtro + texto + grid). Generador IA, APIs, imágenes, navbar/footer: intactos.
- Decisiones: filtro por músculo en la VISTA (filterExercises sin cambio — cero riesgo para los otros consumidores del helper); opciones nunca hardcodeadas; wall-ball descartado por falta de "balón medicinal" en el enum de equipo; la inmutabilidad se validó por deep-compare contra git HEAD (más fuerte que line-diff) porque hay otro agente editando el mismo archivo en paralelo.
---

---
Task ID: 26-d
Agent: calidad-hidratacion
Task: Hidratación segura con reduced-motion (fix del error de consola)

Work Log:
- REPRODUCCIÓN ANTES: agent-browser con `set media light reduced-motion` (verificado matchMedia("(prefers-reduced-motion: reduce)").matches=true; la sesión por defecto arrancaba con reduce=false, por eso hubo que activarlo explícito) → recarga de "/" → dev.log registró `[browser] Uncaught Error: Hydration failed because the server rendered HTML didn't match the client` con diff en <ScrollProgress> (server: div con style scaleX(0) / data-on=null; cliente: null) y <nav> de Navbar (navbar.tsx:90), precedido del warning motion.dev "You have Reduced Motion enabled..." apuntando a navbar.tsx:50:34 (la línea exacta de useReducedMotion()). Evidencia archivada: /tmp/evidencia-hydration-antes.log (133 líneas del dev.log, rango 5033-5165). `agent-browser errors` marcaba ✗. Causa raíz confirmada en framer-motion 12.43.0: su useReducedMotion hace useState(prefersReducedMotion.current) leyendo matchMedia SINCRÓNICAMENTE en el primer render cliente → server (false) ≠ cliente (true) → React regenera el árbol.
- FIX: nuevo src/hooks/use-reduced-motion-safe.ts ("use client"): useReducedMotionSafe(): boolean con estado inicial SIEMPRE false (idéntico en server y primer render) + useEffect que lee window.matchMedia("(prefers-reduced-motion: reduce)").matches, sincroniza solo si difiere y se suscribe a "change" con cleanup (removeEventListener). Export extra useReducedMotionRaw(): boolean | null (valor crudo; null = preferencia aún desconocida) por si algún sitio necesita distinguir "no sé" de "no reduce". Swap quirúrgico en los 4 archivos: hero.tsx, navbar.tsx, scroll-progress.tsx, reveal.tsx — SOLO import + llamada (useReducedMotion() → useReducedMotionSafe()); cero cambios de JSX, clases, textos o animaciones.
- AUDITORÍA del resto del shell SSR (navbar, footer, hero, scroll-progress, reveal, site-shell + logo, cta-button, whatsapp-button, toaster): sin Math.random(), Date.now(), toLocale* ni window/document en render inicial (site-shell y navbar acceden a window solo dentro de effects; navbar ya usa useSyncExternalStore con server snapshot ()=>false, hydration-safe). ÚNICA fuente real hallada: footer.tsx renderizaba `© {new Date().getFullYear()}` (corre durante SSR y primera pintura; solo puede diferir si una carga cruza la medianoche del 31/12). Fix mínimo sin cambio visual: useSyncExternalStore con getServerSnapshot=CURRENT_YEAR (constante de módulo, HTML estable) y getSnapshot=fecha real → React corrige solo si difiere post-montaje (mismo patrón hydration-safe que el scroll del navbar). Nota: la primera versión con useState+useEffect fue reescrita porque eslint (react-hooks/set-state-in-effect) la marcó como 1 error; la final queda 0/0.
- VERIFICACIÓN DESPUÉS (sesión NUEVA de agent-browser, reduced-motion ON): `agent-browser errors` → salida VACÍA, exit 0 (0 errores). console → solo [info] React DevTools + [log] HMR connected; ningún error ni warning de hidratación. dev.log: baseline por wc -l antes de cada carga + grep -i "hydrat" SOLO sobre las líneas nuevas posteriores al fix → 0 coincidencias en todas las cargas de verificación (los errores viejos quedan en el histórico por número de línea, no se tocan). Comportamiento reduced-motion preservado: ScrollProgress ausente post-montaje (querySelector('.origin-left')===null → true), igual que pre-fix tras la regeneración; con reduced-motion OFF el hairline existe y hero-title llega a opacity 1 (animaciones intactas para usuarios sin reduce). Footer: "© 2026 Ezequiel Coaching. Todos los derechos reservados." intacto.
- VISUAL CHECK: /tmp/verif-hydration-home.png (1280×577, reduce ON) y /tmp/verif-hydration-navbar.png (1280×160). Pixel-diff programático (canvas en Chromium, umbral Δ>8 por canal) contra /tmp/verif-before-home.png (captura PRE-fix con reduce ON): 4.152 px dif en 738.560 (0,56%) concentrados 100% en la caja [20,520]-[137,557] de 117×37 px = badge del overlay de dev-tools de Next.js (UI dev-only, no es la app; difiere porque el dev server se reinició entre capturas). Navbar: 0 px dif; footer: 0 px dif; hero: 0 px dif. Dos capturas consecutivas post-fix: 0 px dif (página estable).
- INCIDENTE A DOCUMENTAR: el dev server original (externo a esta sesión) murió solo a las 02:25:56 UTC — dev.log termina en "GET / 200" ya con el fix compilado ("✓ Compiled in 522ms"), sin stack ni error; no fue causado por los cambios (Fast Refresh compiló y sirvió 200 correctamente antes de morir). Se relanzó con el mismo comando (bun run dev -p 3000, salida en append a dev.log) para completar la verificación; el sandbox mata procesos de fondo entre comandos, así que se levantó inline en cada bloque de verificación. NO se tocaron build, BD ni APIs. Actividad paralela de otros agentes visible en dev.log/git (zona API, biblioteca) — no interfiere con el shell.

Stage Summary:
- Archivos: NUEVO src/hooks/use-reduced-motion-safe.ts (useReducedMotionSafe + useReducedMotionRaw). Modificados: src/components/sections/hero.tsx, src/components/site/navbar.tsx, src/components/site/scroll-progress.tsx, src/components/site/reveal.tsx (solo import + hook), src/components/site/footer.tsx (año hydration-safe, texto renderizado idéntico).
- Resultado: errors=0 en sesión limpia con prefers-reduced-motion:reduce (antes: "Hydration failed ... server rendered HTML didn't match" en Navbar/ScrollProgress + regeneración del árbol). lint 0 errores/0 warnings; bunx tsc --noEmit 0. Sin cambios visuales medibles (pixel-diff 0 fuera del badge dev de Next.js); reduced-motion se respeta post-montaje como antes; usuarios sin reduce no ven ninguna diferencia.

---
Task ID: 26
Agent: main (Z.ai Code)
Task: Bloque 26 — ronda 2 de features MIT + calidad (editar/duplicar rutinas, creador manual, metas, heatmap, +50 ejercicios, hidratación segura)

Work Log:
- Auditoría exprés: faltaban creador manual de rutinas (solo existía IA), edición/duplicación, metas propias (agua 2000 fijo, peso sin meta), heatmap de actividad, dataset ampliable y el fix de hidratación documentado en Task 25.
- Lote paralelo 26-a/26-c/26-d:
  * 26-a backend: schema aditivo (ClientProfile.weightGoalKg/waterGoalMl) + PATCH /api/zona/routines/[id] + POST .../duplicate + PATCH /api/zona/profile (metas) + progress.activityDays (90d, regla streak, cero queries extra) + 13 logros (racha-14, sesiones-50, volumen-50t). Smoke tests 9/9; export con metas y sin campos sensibles.
  * 26-c biblioteca: 150 ejercicios (+50 reales clásicos, dedupe contra existentes, 0 strings de músculo nuevos, ids previos intactos por deep-compare vs git HEAD) + 4º filtro "Músculo primario" (29 opciones derivadas en useMemo, nunca hardcode).
  * 26-d hidratación: hook use-reduced-motion-safe.ts (estado inicial false en server y primer render, sync matchMedia en effect) en hero/navbar/scroll-progress/reveal + footer year vía useSyncExternalStore. Antes: hydration error reproducido con reduced-motion ON; después: agent-browser errors = 0, dev.log 0 líneas nuevas con "Hydration failed", pixel-diff navbar/footer/hero = 0 px.
- 26-b secuencial: el subagente agotó contexto DESPUÉS de escribir todo el código (routine-form.tsx nuevo + integraciones en routines-tab/progress-tab/api.ts) pero antes de reportar; verificación completada por el main: lint 0/0, tsc 0, y E2E browser propio (login → crear rutina manual "Verif Push" con dialog + buscador por día → toast "Rutina creada"; Duplicar → "(copia)" + toast; Editar → renombrar → "Rutina actualizada"; meta de agua 2500 → "Tu meta: 2.500 ml al día" + "Meta guardada" persistida tras reload; heatmap "1 día activo" tras registrar 500ml; 13 logros en lista; consola 0 errores; 320px sw==cw con dialog abierto).
- Limpieza final: 9 tablas zona en 0 (incluidos los restos del subagente caído). Server OK (200).
- NOTA proceso: la plataforma mata procesos background entre comandos de subagentes — dos agentes tuvieron que relanzar el dev server inline (documentado en sus entradas); estado final verificado por el main.

Stage Summary:
- Wishlist MIT ronda 2 integrada: rutinas 100% gestionables desde la UI (crear manual/editar/duplicar/eliminar/IA/plan semanal), metas propias de peso y agua, heatmap 90 días, 150 ejercicios con filtro por músculo, 13 logros, hidratación 0 errores para reduced-motion.
- lint 0/0 · tsc 0 · BD vacía · sin push (a pedido).

---
Task ID: 27-b
Agent: general-purpose (subagente gráfica evolución)
Task: Nueva sección "Evolución por ejercicio" en la tab Progreso de Mi Zona: endpoint /api/zona/exercise-trend (lista + puntos e1RM por sesión) + selector con LineChart propio.

Work Log:
- Contexto leído: worklog.md (Tasks 24/25/26), charts.tsx (LineChart/BarChart reusados tal cual, cero cambios), api.ts (zonaApi/ZonaUnauthorized/shortDate/fmtKg), zona-auth.ts (requireZonaProfile/zonaUnauthorized), zona-utils.ts (epley1rm existente: peso×(1+reps/30) redondeado a 1 decimal — reusado sin duplicar), last-performance/route.ts (patrón auth+query+serialización) y progress/route.ts (exerciseName del catálogo). Schema confirmado: WorkoutSession.status "completada" + finishedAt nullable; SetLog.exerciseId/weightKg/reps. NO se tocaron rutas existentes, schema ni charts.tsx.
- Backend NUEVO src/app/api/zona/exercise-trend/route.ts (GET, force-dynamic): auth primero (401 igual al resto), después validación. SIN ?exerciseId= → { exercises: [{ exerciseId, exerciseName, sessions }] } de sets en sesiones completadas (status "completada" AND finishedAt not null), sessions = Set de sessionIds distintos por ejercicio (N series de una sesión cuentan 1), sort por exerciseName localeCompare "es". CON ?exerciseId=X → { points: [{ date: finishedAt ISO, e1rm, weightKg, reps }] }: mejor set por sesión según epley1rm (empate → mayor peso), ascendente por fecha; series sin peso no generan punto (honesto); ejercicio sin datos → points: [] sin error. ?exerciseId= presente pero vacío (tras trim) → 400 {error}. Sin take límite (escala personal).
- api.ts ADITIVO (después de LastPerformanceDTO): ExerciseTrendItemDTO, ExerciseTrendPointDTO, ExerciseTrendListDTO, ExerciseTrendDTO + fetchExerciseTrendList() y fetchExerciseTrend(exerciseId) sobre zonaApi (401 → ZonaUnauthorized automático, encodeURIComponent del id).
- progress-tab.tsx ADITIVO: nueva sección <ExerciseTrendSection onActionError={...}/> como Card full-width entre la grilla (volumen semanal último card) y el heatmap. Estados: lista al montar vía useEffect con setState SOLO en .then/.catch (0 setState síncrono en effect body; retry con contador trendListRetry en deps); select nativo de la casa (@/components/ui/input Select, min-h-11, opciones "Nombre (N sesión/es)"); al elegir → cache Map por exerciseId + guard de respuestas obsoletas (ref trendReqId, incrementa en TODA selección incl. cache hits); loading (Loader2 chico + aria-live), error no-401 con botón Reintentar (401 → onActionError → AuthGate, patrón de la casa); <2 puntos → EmptyState honesto "Necesitás al menos 2 sesiones con este ejercicio para ver la evolución" (no gráfica vacía); ≥2 → LineChart {label: shortDate(date.slice(0,10)), value: e1rm} + ariaLabel descriptivo ("de X a Y kg en N sesiones"); lista vacía → EmptyState; placeholder sin selección. track("zona_exercise_trend", {exerciseId, points}) en fetch fresco exitoso. Todo es-AR ("elegí", "completá"), sin azul/índigo (tokens de tema, acento volt vía stroke/fill-primary), responsive (chart w-full como los existentes).
- Verificación: bun run lint → 0 errores/0 warnings (exit 0). bunx tsc --noEmit → 0 errores (exit 0). Dev server vivo (NO reiniciado): curl sin cookie GET /api/zona/exercise-trend → 401 {"error":"Iniciá sesión para continuar."} y ?exerciseId= → 401 (auth primero, igual que las demás rutas); dev.log muestra "✓ Compiled" + los 3 requests 401, sin errores de compilación. NO se sembraron datos (E2E del principal), NO se usó bun run build, NO se instaló nada.

Stage Summary:
- La tab Progreso ganó "Evolución por ejercicio": selector de ejercicios con sesiones completadas → curva e1RM (Epley) del mejor set de cada sesión completada, con cache, guard de stale responses, estados honestos (loading/error-retry/empty/<2 puntos) y el mismo LineChart del proyecto.
- Archivos: NUEVO src/app/api/zona/exercise-trend/route.ts; MODIFICADOS (solo aditivos) src/components/zona/api.ts (DTOs + 2 fetchers) y src/components/zona/progress-tab.tsx (importos + render de la sección + componente ExerciseTrendSection). charts.tsx, rutas existentes, schema y package.json: intactos.
- Shapes exactos para E2E: lista {"exercises":[{"exerciseId":"press-banca","exerciseName":"Press banca","sessions":2},...]}; puntos {"points":[{"date":"2026-02-10T18:34:12.000Z","e1rm":82.5,"weightKg":70,"reps":8},...]} ascendente por fecha; errores {"error":"..."} con 400 (exerciseId vacío) / 401 (sin sesión).
- lint 0/0 · tsc 0 · BD sin sembrar · dev server intacto.

---
Task ID: 27-a
Agent: frontend-plan-hoy
Task: Widget "Plan de hoy" en Mi Zona: card compacto encima de las tabs que lee GET /api/zona/schedule, resuelve el día actual con el mapping del backend (0=Lunes..6=Domingo) y muestra la asignación de hoy con CTA a Entrenar / descanso / plan vacío, con loading skeleton y error con Reintentar.

Work Log:
- Contexto leído: worklog.md (Tasks 24/25/26: patrones de zona-view, WeeklyPlan de 25-e que ya consume /api/zona/schedule, reglas de hidratación de 26-d), zona-view.tsx (switchTab, handleActionError, dataReady, montaje), api.ts (ScheduleSlotDTO ya existía → NO hubo que tocarlo), schedule/route.ts (serializeWeek: weekday 0=Lunes..6=Domingo, shape {weekday, routineId, routineTitle, day}), train-tab.tsx (props), ui/card, ui/button, site/states (Skeleton/ErrorState).
- Creado src/components/zona/today-widget.tsx ("use client", aislado): Card compacto con 5 estados completos — (1) loading: skeleton con role="status" aria-live (motion-reduce:animate-none, el widget no anima nada propio); (2) error: ErrorState con Reintentar (retry = handler que re-ejecuta el fetch; el weekday se recalcula en la continuación del nuevo fetch); (3) hoy asignado: eyebrow "Plan de hoy · {día}", nombre de rutina (truncate) + chip "Día N" + Button volt "Entrenar ahora" (min-h-11, w-full sm:w-auto, aria-label descriptivo con rutina+día); (4) hoy libre con plan existente: "Hoy: descanso" (role="status", Moon) + Button outline "Ver plan semanal"; (5) plan vacío (0 slots con routineId): "Todavía no armaste tu plan semanal" + Button volt "Armar mi plan". Ambos van a la tab Rutinas.
- Mapping weekday: hoy = (new Date().getDay() + 6) % 7 → 0=Lunes..6=Domingo, idéntico al contrato del backend (schedule/route.ts: zod "0 (lunes) a 6 (domingo)" + serializeWeek). Etiquetas es-AR en const local (mismo criterio "constantes duplicadas a propósito" de 26-a; routines-tab no exporta su WEEKDAYS y no se podía tocar).
- HIDRATACIÓN (regla dura): todayWeekdayIndex() se llama SOLO dentro de la continuación .then del fetch (nunca en render, nunca síncrono en el cuerpo del effect). El effect solo arranca la promesa; todo setState vive en .then/.catch/.finally o en handlers (react-hooks/set-state-in-effect = 0). Estado "hoy" guardado en state (todayWeekday: number | null, null hasta responder). Protección anti-respuesta-obsoleta con ref de reqId incremental (cubre también retries superpuestos, mismo objetivo que el flag alive de la casa).
- 401: ZonaUnauthorized → onActionError (handleActionError de zona-view → resetToAnon/AuthGate), el patrón exacto de WeeklyPlan en routines-tab (25-e). Resto de errores → mensaje local + Reintentar.
- Cambio de tab: SOLO callbacks onGoToTrain/onGoToRoutines cableados a switchTab("entrenar")/switchTab("rutinas") existentes en zona-view. DECISIÓN (CONSERVAR > CAMBIAR): NO se preselecciona rutina+día en TrainTab porque TrainTab exige sesión activa y TrainEmpty no tiene props de preselección — modificarlo quedaba fuera del alcance permitido (solo today-widget/zona-view/api). El CTA además trackea cta_click {label: zona-plan-hoy-entrenar / -ver-semana / -armar} siguiendo el patrón "zona-auth-ok".
- Montaje aditivo en zona-view.tsx: import + <TodayWidget> dentro del branch dataReady, ENCIMA del <nav role="tablist"> (visible en todas las tabs, no se desmonta al cambiar de tab). Tabs, vistas y handlers intactos; JSDoc de la vista actualizado con 1 línea.
- Responsive 320px: fila flex-wrap (icono shrink-0 + texto flex-1 min-w-0 con truncate + CTA w-full que baja a su propia fila en mobile, sm:w-auto en ≥sm); sin anchos fijos que desborden (skeletons con max-w-full). Sin colores blue/indigo: solo primary (volt #bdef27), muted y destructive del tema.
- NO instalé paquetes, NO toqué schema, NO toqué api.ts (el DTO ScheduleSlotDTO ya existía), NO levanté/reinicié dev server, NO sembré datos.

Stage Summary:
- Archivos: NUEVO src/components/zona/today-widget.tsx (~190 líneas); MODIFICADO src/components/views/zona-view.tsx (3 cambios aditivos: import, JSDoc, montaje encima de las tabs). api.ts, train-tab, routines-tab, schedule route y schema: INTACTOS. Nota: en el árbol hay cambios en paralelo de otro agente (api.ts +43, progress-tab.tsx, api/zona/exercise-trend/) que NO son de esta tarea y no se tocaron; lint+tsc corrieron sobre el árbol combinado y pasan.
- Verificación estática: bun run lint → 0 errores, 0 warnings (sin output). bunx tsc --noEmit → exit 0. Verificación de navegador la hace el principal (no había server corriendo y no se levantó).
- Estados soportados: loading (skeleton) / error con Reintentar (re-fetch + weekday recalculado) / hoy asignado (rutina + Día N + Entrenar ahora) / hoy libre con plan ("Hoy: descanso" + Ver plan semanal) / plan vacío (Armar mi plan). Slot con routineId pero day null (permitido por el PUT) se muestra sin chip "Día N". Slot con rutina borrada (FK onDelete: SetNull → routineId null) cae en "descanso", honesto.
- Limitación documentada: el widget hace fetch al montar (dentro de dataReady) + retry; NO se re-sincroniza en vivo cuando se edita el plan en Rutinas (requeriría tocar routines-tab o añadir estado en zona-view, fuera del alcance conservador). Aparece actualizado al recargar o tras un retry. Si el principal quiere sync en vivo: pasar tab como prop y refetch silencioso (1 prop + 1 effect).

---
Task ID: 27-c
Agent: biblioteca-200
Task: Ampliar la biblioteca de ejercicios de 150 a 200 con 50 ejercicios reales nuevos

Work Log:
- Contexto leído: worklog.md (Tasks 24-b/25/26: contrato de exercises.ts — primaryMuscles 1-2, secondaryMuscles ≤3, 4-6 pasos, 1-2 tips, ids kebab-case únicos e INMUTABLES porque son llaves de SetLog.exerciseId; 26-c: convención de ampliación y vocabulario de músculos) y src/lib/content/exercises.ts completo (150 entradas, 7 arrays de grupo + EXERCISES por spreads, helper filterExercises).
- Inventariado previo con script bun: 48 strings de músculo, 7 valores de equipment, 7 grupos, 3 niveles; listado id|name|equipment|level de las 150 entradas para dedupe.
- DEDUPE ESTRICTO: 50 candidatos clásicos elegidos solo si no existían (case-insensitive y también normalizados sin acentos; varios descartados por colisión cercana: wall ball y jump rope por falta de valor de equipment compatible — se mantiene el criterio de 26-c de NO expandir enums —, hip thrust/nordic/face pull/búlgara/remo banco inclinado/lateral polea ya existían, etc.).
- ESTRUCTURA (decisión clave): en vez de insertar dentro de cada array de grupo (26-c), los 50 nuevos van en un const independiente `ampliacion27c` ANTES del "── Dataset final" y se anexan AL FINAL de EXERCISES con UNA línea nueva `...ampliacion27c,` tras `...fullBody,`. Así las primeras 150 entradas del dataset público conservan índice, orden y contenido byte a byte (validado por JSON.stringify elemento a elemento vs git HEAD), y el diff del archivo queda 100% aditivo (987 inserciones / 0 deletions, 0 líneas "-").
- Valores cerrados: CERO strings nuevos de músculo/grupo/equipo/nivel — verificado por script contra los sets computados desde `git show HEAD:src/lib/content/exercises.ts`.
- Conteos hardcodeados: `rg -n "150|ciento cincuenta" src/` → biblioteca-view.tsx:203 ("Más de 150 ejercicios…" en PageHeader) actualizada a 200; charts.tsx:19 (`const H = 150`, altura de gráfico) y achievements/route.ts:78 (`ml >= 1500`) NO son conteos de ejercicios y quedaron intactos; el badge "{EXERCISES.length} ejercicios · 7 grupos musculares" ya es dinámico. En exercises.ts se AÑADIÓ una línea de doc-comment (adición pura) anunciando el bloque 27-c; la línea histórica "~150" no se tocó para mantener 0 deletions.
- VALIDACIÓN (script bun temporal en /home/z/tmp-27c/, borrado al final): (1) HEAD=150 / CUR=200 / nuevos=50; (2) CUR.slice(0,150) byte-idénticas a HEAD entrada por entrada y en orden; (3) 0 ids duplicados en 200; (4) 0 nombres duplicados case-insensitive y 0 tras normalizar NFD (sin acentos); (5) todo group/equipment/level/músculo dentro de los sets de HEAD; (6) contrato 24-b en las 200 entradas (primary 1-2, secondary 1-3, instrucciones 4-6, tips 1-2, sin strings vacíos); (7) ids nuevos kebab-case (el check se limita a los nuevos: `remo-T-barra` es un id PREEXISTENTE de HEAD e intocable). OUTPUT: "OK - 200 ejercicios…". Smoke runtime: getExerciseById/exerciseName/filterExercises funcionan con 200 (filter q=landmine → press-landmine + rotaciones-landmine).
- Verificación: git diff --numstat exercises.ts → 987 0 (solo adiciones; grep "^-[^-]" del diff → 0 líneas); biblioteca-view.tsx → 1 línea (la copia de conteo); bunx tsc --noEmit → exit 0; bun run lint → 0 errores/0 warnings (árbol combinado con cambios en paralelo de otro agente: today-widget/zona-view/exercise-trend, que no se tocaron).
- Limpieza: script de validación y /home/z/tmp-27c/ borrados.

Stage Summary:
- Total ejercicios: 200 (pecho 18→26, espalda 23→30, piernas 30→38, hombros 18→25, brazos 21→28, core 22→29, full-body 18→24). Nivel de los 50 nuevos: 19 principiante / 20 intermedio / 11 avanzado.
- Los 50 nuevos (todos reales y clásicos), por grupo:
  * Pecho (8): press de suelo con mancuernas (floor press), press de banca en multipower (Smith), press de pecho con banda, cruzamiento de poleas desde abajo, aperturas en polea a una mano, flexiones archer, flexiones con palmada, fondos en paralelas lastrados.
  * Espalda (7): jalón al pecho agarre neutro, jalón en polea a una mano, remo Pendlay, remo gorila, remo en máquina con apoyo pectoral (chest supported), pullover con barra, muscle-up en barra.
  * Piernas (8): sentadilla sumo con kettlebell, zancada trasera (reverse lunge), pull-through en polea, hip thrust con banda, sentadilla en multipower, sentadilla sissy, elevación de gemelos a una pierna, caminata lateral con banda (monster walk).
  * Hombros (7): press landmine, press de hombros con kettlebell, Z-press con barra, remo al mentón en polea, apertura inversa con banda, molino con kettlebell (windmill), apoyo invertido en pared (wall handstand).
  * Brazos (7): curl en barra Z (EZ), curl araña (spider curl), curl Zottman, curl arrastrado (drag curl), curl martillo en polea, extensión de tríceps con cuerda en polea, colgado con toalla (towel hang).
  * Core (7): limpiaparabrisas (windshield wipers), dragon flag, plancha de Copenhagen, elevación de piernas en paralelas (silla romana), crunch oblicuo, crunch en máquina, rotaciones con landmine.
  * Full-body (6): arranque con barra (snatch), envión (split jerk), oruga (inchworm), salto de patinador (skater jump), burpee con salto largo (burpee broad jump), rodillas altas (high knees).
- Archivos tocados: src/lib/content/exercises.ts (SOLO adiciones: línea de doc + const ampliacion27c + spread) y src/components/views/biblioteca-view.tsx (1 string de conteo 150→200). Ids/orden de las 150 previas intactos (verificados byte a byte vs HEAD); sin cambios en enums, filtros, generador IA, APIs, imágenes ni otros consumidores.
- Decisiones: ampliación como bloque final + spread (garantiza "primeras 150 byte-idénticas" y diff 100% aditivo, más fuerte que el criterio de 26-c); refuerzo de equipamiento con banda/kettlebell/poleas donde el dataset era fino; ejercicios olímpicos (snatch/envión) y calisténics avanzados (muscle-up, dragon flag, limpiaparabrisas) marcados como avanzado con progresiones en los tips.
---
Task ID: 27
Agent: main (Z.ai Code)
Task: Push pendiente a GitHub (Tasks 24+25+26) + Bloque 27 (widget "Plan de hoy", evolución por ejercicio, +50 ejercicios) + E2E consolidado + push final

Work Log:
- PUSH 1: el remoto estaba en c2dd2e0 (Task 24). 2 commits locales sin subir (auto-commits 534fd17 y 2ebc87b) contenían TODAS las Tasks 25 y 26. Escaneo de secretos del diff (ghp_/github_pat_/sk-/JWT/claves AWS/.env/db/tool-results) = 0 coincidencias; .gitignore intacto. Push con token clásico embebido → remoto 2ebc87b == local (ls-remote verificado). El token sigue activo (el usuario NO lo revocó aún).
- Auditoría: Task 26 ya integrada en esos commits (routine-form, duplicate, metas, heatmap, 150 ejercicios, fix hidratación). Definido bloque 27 con 3 sub-agentes paralelos sobre archivos disjuntos.
- 27-a (subagente OK): NUEVO src/components/zona/today-widget.tsx — widget "Plan de hoy" montado en zona-view encima de las tabs: 5 estados (skeleton/empty "Armar mi plan"/asignado con CTA "Entrenar ahora"/"Hoy: descanso" con "Ver plan semanal"/error con Reintentar). Weekday calculado SOLO en .then() del fetch (hydratación-safe), mapping (getDay()+6)%7 idéntico al backend; 401 vía onActionError→AuthGate; tracking cta_click zona-plan-hoy-*.
- 27-b (subagente OK): NUEVO src/app/api/zona/exercise-trend/route.ts (GET lista {exercises:[{exerciseId,exerciseName,sessions}]} de sesiones completadas; GET ?exerciseId= → {points:[{date,e1rm,weightKg,reps}]} 1 punto/sesión con mejor set Epley, asc por fecha, 401/400) + fetchExerciseTrendList/fetchExerciseTrend en api.ts + sección "Evolución por ejercicio" en progress-tab (select con "(N sesiones)", LineChart propio con aria-label, cache Map + descarte de respuestas obsoletas, empty honesto si <2 sesiones). Curl sin cookie → 401 verificado.
- 27-c (subagente OK, reportó tarde pero trabajo completo): exercises.ts 150→200 en bloque final "ampliacion27c" + spread (primeras 150 byte-idénticas vs HEAD, diff 987 inserciones / 0 borrados); dedupe case-insensitive+NFD; cero strings nuevos de músculo/grupo/equipo/nivel (sets validados contra HEAD); copia de biblioteca-view 150→200; badge dinámico ya mostraba 200; contrato 24-b validado en las 200 entradas.
- E2E consolidado (agent-browser, sesión real): registro de perfil → widget empty OK → crear rutina "Verif 27" (manual dialog, buscador "banca", Press de banca con barra) → plan semanal Jueves→Día 1 → reload (open no recarga; RELOAD sí) → widget "Plan de hoy · Jueves / Verif 27 / Día 1 / Entrenar ahora" → CTA cambia a tab Entrenar → sesión 1: 60×8 (PR, RestTimer 1:29=90s de rutina, finalizada con 480 kg volumen) → sesión 2: 65×8 (520 kg) → Progreso: sección "Evolución por ejercicio" con "Press de banca con barra (2 sesiones)" → gráfica SVG con aria-label exacto "Evolución de e1RM en Press de banca con barra: de 76 a 82,3 kg en 2 sesiones" (Epley exacto) → quitar Jueves → widget empty coherente → asignar Viernes → widget "Hoy: descanso" ✅ → Biblioteca: "Más de 200 ejercicios" + badge "200 ejercicios · 7 grupos musculares" → 320px: sw==cw==320 y 0 offenders en Mi Zona y Biblioteca → agent-browser errors vacío, consola sin errores/hidratación → dev.log 0 líneas nuevas con hydrat|error tras carga fresca.
- Limpieza: BD a 0 en las 9 tablas (script Prisma, verificado por counts). HOME 200, exercise-trend 401 sin sesión. lint 0/0, tsc 0.
- PUSH 2: commit con Task 27 subido a GitHub (mismo método token embebido), remoto == local verificado con ls-remote.

Stage Summary:
- Repo GitHub sincronizado con TODO el trabajo (Tasks 24→27). Widget "Plan de hoy" (retención diaria), evolución de e1RM por ejercicio (gráfica accesible propia, sin dependencias nuevas), biblioteca de 200 ejercicios reales.
- Estados del widget 100% verificados en browser real; endpoint trend 200/401/400 según contrato; E2E completo con datos reales sembrados y BD devuelta a 0.
- PENDIENTE usuario: revocar el token ghp_...dzbo (sigue activo) y emitir uno nuevo si se van a seguir pusheando cambios.
---
Task ID: 28-restauracion
Agent: main (Z.ai Code)
Task: Recuperación del proyecto tras reset del sandbox + inicio de restyling "estilo FitSync" (dark teal + esmeralda)

Work Log:
- INCIDENTE: al comenzar Task 28 el workspace estaba restaurado a un snapshot viejo (HEAD en auto-commit 0883846): desaparecieron src/components/zona, src/app/api/zona, biblioteca-view, exercises.ts de 200, worklog de Tasks 25-27, imágenes de ejercicios; el remote origin fue eliminado de la config local y el dev server servía el código viejo (/api/products).
- RECUPERACIÓN: backup de .env/Caddyfile/db a /tmp/backup-restore → git remote add origin → fetch → git reset --hard origin/main (a1eaef0, estado verificado de Task 27) → restauración de .env y Caddyfile → bun install (160 paquetes) → bunx prisma db push (16 tablas recreadas, 0 perfiles) → restart del dev server (el viejo quedó con estado mixto: 500 en / y /manifest.webmanifest) → verificación: HOME 200, exercise-trend 401 sin sesión, manifest 200.
- Lección: el sandbox puede restaurar snapshots entre sesiones; el push a GitHub fue la red de seguridad — sin él se perdía todo.

Stage Summary:
- Proyecto restaurado 1:1 al estado a1eaef0 (Task 27 completa). BD vacía. Servidor sano.
---
Task ID: 28
Agent: main (Z.ai Code)
Task: Restyling completo al estilo "app fitness pro" (referencia FitSync del usuario): dark teal + esmeralda/mint

Work Log:
- DECISIÓN DE ALCANCE: el estilo FitSync (dark teal profundo + acento esmeralda + bordes sutiles + glows + radios suaves) se implementa a nivel de TOKENS, no tocando componente por componente — toda la app usa clases semánticas (bg-background, text-primary, stroke-primary...), así que un swap de paleta en globals.css recolorea las 35+ vistas sin riesgo funcional.
- FASE 1 (globals.css): --background oklch(0.14 0.012 190) teal oscuro; superficies/card/popover/border/input con tinte teal (hue 190); --primary esmeralda mint oklch(0.84 0.17 162) ≈ #3ddc97 con foreground verde-negro; selection/focus/scrollbar/glow-volt/bg-brand-halo actualizados al mismo verde; --radius 0.625rem → 1rem (look más suave y redondeado, tipo mockup FitSync).
- Colores de sistema: themeColor (layout.tsx), background/theme_color (manifest.ts) y fondo de la página offline (sw.js) → #0b1315.
- FASE 2 (assets de marca): recolor programático con PIL (hue-shift +83°, solo píxeles con saturación>0.25) en og.png, icon-512, icon-512-rounded, icon-192, apple-touch-icon, logo-mark — el stripe/texto/punto volt pasaron a esmeralda conservando el diseño píxel a píxel (verificado visualmente). logo-full.png (blanco puro) y las 7 imágenes de ejercicios (ya eran verde esmeralda) intactas. Falsas alarmas descartadas: /brand/* sirve 200; los 404 de /og.png e /icon-512.png venían del manifest viejo cacheado por el navegador de pruebas.
- Smoke E2E post-restyle: registro en Mi Zona (detectado: click con ref stale no dispara; click por eval funciona) → zona con 5 tabs + widget "Plan de hoy" empty state OK → calculadora IMC: 78kg/178cm → "24,6" exacto → biblioteca OK → 320px sw==cw==320 en home/mi-zona/calculadoras → agent-browser errors vacío. Captura fullpage "vacía" diagnosticada: los Reveal animan on-scroll (no disparan en captura fullpage); scrolleando de verdad las secciones renderizan completas (evidencia /tmp/restyle-mid.png).
- Limpieza: perfil de prueba borrado → 9 tablas en 0. lint 0/0 · tsc 0 · HOME 200.

Stage Summary:
- Toda la web (público + Mi Zona + herramientas) con el estilo profesional dark teal + esmeralda pedido (referencia FitSync), sin tocar lógica ni JSX: cambio a nivel tokens + assets de marca recoloreados in-place.
- Sin datos de prueba (BD en 0). PENDIENTE: push a GitHub (token ghp_ sigue activo).
